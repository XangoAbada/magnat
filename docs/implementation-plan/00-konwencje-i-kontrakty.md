# 00 — Konwencje i kontrakty wspólne (dokument nadrzędny)

Status: obowiązujący dla wszystkich faz M0–M12.
Źródło: `PRD_Magnat.md` §16, §17, §18.

Ten dokument ustala decyzje, które **nie mogą być podejmowane osobno w każdej fazie**.
Jeśli plan fazy potrzebuje czegoś, co jest tu zdefiniowane — odwołuje się do tego dokumentu,
nie redefiniuje. Jeśli faza potrzebuje zmiany kontraktu — zgłasza to w sekcji
„Decyzje otwarte" swojego dokumentu, a zmiana ląduje tutaj.

---

## 1. Indeks faz i własność modułów

Każdy crate ma **jednego właściciela** (fazę, która go tworzy). Inne fazy mogą go rozszerzać,
ale nie projektują go od nowa.

| Faza | Dokument | Crate'y tworzone (właściciel) | Crate'y rozszerzane |
|---|---|---|---|
| M0 | `M0-fundament-silnika.md` | `engine/core`, `engine/ecs`, `engine/jobs`, `engine/io` (minimalny), `engine/devtools` (szkielet), `tools/headless` | — |
| M1 | `M1-swiat-statyczny.md` | `engine/voxel`, `engine/render`, `sim/world` (teren, hydrologia, klimat) | `core`, `ecs`, `devtools` |
| M2 | `M2-miasto-statyczne.md` | `engine/spatial` | `sim/world` (drogi, strefy, parcele, budynki), `render` (nakładki) |
| M3 | `M3-ludzie-i-dzien.md` | `sim/agents`, `engine/ui` (szkielet + karta inspekcji) | `sim/world` (populacja) |
| M4 | `M4-ruch.md` | `engine/nav`, `sim/traffic` | `sim/agents` (wybór środka transportu) |
| M5 | `M5-gospodarka-detaliczna.md` | `sim/economy`, `tools/balansator` | `sim/agents` (decyzja zakupowa), `sim/firms` (szkic sklepu) |
| M6 | `M6-lancuch-dostaw.md` | `sim/supply` | `sim/economy` (rynek B2B), `sim/firms` (produkcja) |
| M7 | `M7-firmy-ai-i-rynek-pracy.md` | `sim/firms` (pełny), **`sim/policy`** (silnik reguł — K-11; język projektuje M9) | `sim/economy` (moduł `labor`), `sim/supply` (pokrycie etatowe — `K-44`), `data/jobs/` (wagi produktywności — `K-43`), `data/tuning/` (`labor.ron` — kalibracja rynku pracy, `K-35`), **`sim/macro`** (podzbiór „co jeśli" wg kontraktu M10 §6 — crate powstaje w M7f i należy do M10, `K-54`) |
| M8 | `M8-miasto-jako-aktor.md` | `sim/city`, `sim/events` | `sim/traffic` (sieci przesyłowe) |
| M9 | `M9-gracz-kariera.md` | `game/`, `engine/ui` (panele biznesowe, automatyzacja polityk) | wszystkie `sim/*` (hooki gracza) |
| M10 | `M10-glebia.md` | `sim/macro` (pełny, historia „na sucho") | `sim/firms` (marka, R&D, giełda, media), `sim/economy` |
| M11 | `M11-prezentacja.md` | `engine/audio`, **`sim-snapshot`** (jedyny kanał sim → render; osobny crate, żeby zakaz mutacji był egzekwowany grafem zależności, a nie regulaminem) | `engine/render`, `engine/voxel` (LOD, animacje, wnętrza) |
| M12 | `M12-skala-i-jakosc.md` | `engine/script` (modding) | `engine/io` (pełny), wszystkie (profilowanie, pamięć, lokalizacja) |

`engine/io` w wersji minimalnej (snapshot ECS + hash stanu) powstaje w M0; pełne wersjonowanie
schematu, migracje i zapis w tle — M12.
`engine/devtools` rośnie przyrostowo: właścicielem szkieletu jest M0, każda faza dodaje własny inspektor.

---

## 2. Typy bazowe (`engine/core`) — kontrakt nienegocjowalny

```rust
// Pieniądz: zawsze i64 w groszach. Nigdy f32/f64, nigdy w komponentach jako float.
pub struct Money(pub i64);              // 1 = 1 grosz

// Czas symulacji
pub struct SimMinute(pub u64);          // minuty od startu świata (tick ekonomiczny = 1)
pub struct SimInstant(pub u64);         // milisekundy czasu gry (tick ruchu mikro = 100 ms)
pub struct Tick(pub u64);               // licznik ticków ekonomicznych, monotoniczny

// Wielkości fizyczne — całkowitoliczbowe
pub struct Mass(pub i64);               // gramy
pub struct Volume(pub i64);             // mililitry
pub struct Energy(pub i64);             // watogodziny
pub struct Qty(pub i64);                // milisztuki (sztuki × 1000)

// Skale
pub struct Q(pub u8);                   // jakość, zaspokojenie, umiejętność: 0..=100
pub struct Mood(pub i8);                // -100..=100

// Identyfikatory: typowane newtype'y nad indeksem ECS z generacją
pub struct CitizenId(pub Entity);
pub struct HouseholdId(pub Entity);
pub struct FirmId(pub Entity);
pub struct SiteId(pub Entity);          // zakład
pub struct BuildingId(pub Entity);
pub struct ParcelId(pub Entity);
pub struct VehicleId(pub Entity);
// UWAGA (K-16): partie i oferty NIE są encjami ECS — to uchwyty do dedykowanych aren.
pub struct BatchId { index: u32, generation: NonZeroU32 }   // arena partii (sim/supply)
pub struct OfferId { index: u32, generation: NonZeroU32 }   // arena ofert  (sim/economy)
pub struct ContractId(pub Entity);
pub struct DistrictId(pub u16);
pub struct GoodId(pub u16);             // indeks do katalogu danych, stabilny w obrębie wersji danych
pub struct RecipeId(pub u16);
pub struct JobRoleId(pub u16);
```

Zasady:
- Arytmetyka pieniądza: `checked_add`/`checked_mul` + jawne zaokrąglanie (`div_round_half_up`),
  nigdy niejawne obcinanie. Podział kwoty między N stron musi sumować się do oryginału
  (reszta trafia do pierwszego wg ustalonego porządku).
- Floaty **wolno** stosować w geometrii, renderze, fizyce ruchu i funkcjach użyteczności.
  **Nie wolno** w: pieniądzu, księgowości, stanach magazynowych, podatkach.
- Każdy float, którego wynik wpływa na stan trwały, musi być sumowany w ustalonej kolejności
  (iteracja po posortowanym kluczu, nigdy po `HashMap`).

---

## 3. Determinizm — reguły wiążące wszystkie fazy (PRD §18.2)

1. **RNG:** `xoshiro256++`, strumień wyprowadzany funkcją czystą:
   `rng(world_seed, StreamId, entity_index, tick)`. Brak globalnego stanu RNG.
   `StreamId` to enum w `core` — faza dopisuje własne warianty, nigdy nie zmienia istniejących wartości.
2. **Kolekcje:** zakaz iterowania po `std::collections::HashMap`/`HashSet` w kodzie symulacji.
   Dozwolone: `Vec`, `BTreeMap`, własna `SeededMap` o deterministycznej kolejności.
3. **Równoległość:** wynik nie może zależeć od kolejności ukończenia jobów.
   Redukcje — deterministyczne fork-join (składanie po indeksie chunka, nie po kolejności zakończenia).
4. **Mutacje strukturalne:** wyłącznie przez bufory komend, aplikowane w punktach synchronizacji,
   posortowane po `(SystemId, entity_index)`.
5. **Brak zależności od czasu rzeczywistego** w kodzie symulacji (żadnego `Instant::now()`).
6. **Test kontraktowy:** hash stanu ECS co 1000 ticków; dwa przebiegi tego samego seeda =
   identyczny ciąg hashy. Każda faza dopisuje swoje komponenty do funkcji haszującej —
   to element Definition of Done.

---

## 4. Architektura czasu i LOD (PRD §17.1, §17.4)

- Tick ekonomiczny: **1 minuta gry**. System deklaruje częstotliwość:
  `EveryMinute`, `EveryHour`, `EveryDay`, `EveryMonth`.
- Tick ruchu mikro: **100 ms gry**, tylko dla encji w LOD Mikro.
- Render: niezależny, interpolacja z podwójnie buforowanego snapshotu.
- **Zasada LOD** (doprecyzowana po analizie M4 — poprzednia wersja była niewykonalna dla makro):

  **Mikro ↔ mezo: tolerancja 0.** Wynik ekonomiczny nie zależy od poziomu. Każdy system działający
  w mikro ma mezo-odpowiednik dający ten sam czas, to samo zużycie i **ten sam koszt pieniężny**.
  Mezo jest źródłem prawdy; mikro jest wizualizatorem bez prawa zapisu do stanu ekonomicznego.
  Uzasadnienie: zbiór encji w mikro zależy od kamery gracza, a kamera nie wchodzi do hasha stanu —
  gdyby mikro wpływało na ekonomię, obrót kamerą zmieniałby saldo gospodarstwa domowego
  i determinizm (§3) upadałby.

  **Makro ↔ mezo: kontrakt słabszy, ale z twardym rdzeniem.** Makro agreguje (dzielnica × klasa),
  więc równość co do grosza jest z definicji nieosiągalna. Obowiązuje za to:
  1. **Zachowanie pieniądza i masy — tolerancja 0.** Makro nie tworzy ani nie niszczy
     ani grosza, ani grama. To nie podlega negocjacji.
  2. Odchylenie agregatów (ceny, wolumeny, zatrudnienie) ≤ 0,5% w skali miesiąca gry —
     **wyłącznie dla agregatów o liczebności n ≥ 500**. Dla mniejszych zbiorów, a w szczególności
     dla pojedynczej firmy, błąd rzędu 3–12% jest nieusuwalny (wariancja rozkładu wielomianowego)
     i nie jest kwestią kalibracji. Konsekwencja wiążąca dla konsumentów makro: wynik `what_if()`
     wolno używać **porównawczo** (uporządkowanie wariantów z marginesem), nigdy jako liczby
     bezwzględnej w regule progowej ani jako wartości pokazanej graczowi. Model deklaruje własny
     błąd razem z wynikiem i ma test na uczciwość tej deklaracji w obie strony.
  3. **Brak dryfu systematycznego:** błąd nie może się kumulować w jedną stronę —
     test 100 lat gry porównuje trajektorie makro i mezo.
  4. Przejście makro→mezo odtwarza stany indywidualne deterministycznie z seeda i tożsamości.

  Właściciele: M4 (mikro↔mezo), M10 (makro, `sim/macro`), M12 (tryb 50× i test długich sesji).

---

## 4a. Rozstrzygnięcia koordynacyjne (wiążące, dopisywane w trakcie)

Decyzje, których pojedyncza faza nie mogła podjąć sama. Numeracja `K-n`.

| # | Sprawa | Rozstrzygnięcie | Dotyczy |
|---|---|---|---|
| K-1 | Kalendarz gry: 360 dni (12×30) vs gregoriański | **360 dni, 12 miesięcy × 30.** Miesiąc jest jednorodną jednostką tickową, co upraszcza podatki, wybory, sezony i amortyzację. Brak lat przestępnych. Data wyświetlana graczowi może być kosmetycznie mapowana na kalendarz realny, ale symulacja liczy w 360 | M0 (właściciel `SimCalendar`), M1, M8, M10 |
| K-2 | Właściciel grafu pieszego | **`engine/nav` należy do M4.** M3 tworzy minimalny graf pieszy (odległości i czasy dojścia) za tym samym interfejsem, M4 zastępuje go pełną implementacją i dokłada routing multimodalny. M3 nie projektuje własnego, równoległego API | M3, M4 |
| K-3 | Bootstrap GPU | M0 buduje `wgpu` w `tools/voxelview` jako rusztowanie do wyrzucenia; **M1 przenosi `init_gpu()` do `engine/render`** i od tego momentu jest właścicielem urządzenia | M0, M1 |
| K-4 | Zakresy `StreamId` | Rezerwacja blokami po 20 per faza: M0 0–19, M1 100–119, M2 120–139, M3 140–159, M4 160–179, M5 180–199, M6 200–219, M7 220–239, M8 240–259, M9 260–279, M10 280–299, M11 300–319, M12 320–339. Fazie, której 20 wartości nie wystarcza, przysługuje **blok nadmiarowy `baza + 1000`** (M2: 1120–1219) — bez przenumerowywania czegokolwiek. Wartości raz nadane są niezmienne, bo zmiana numeru strumienia zmienia każdy świat wygenerowany wcześniej z tego samego seeda | wszystkie |
| K-6 | Funkcje przestępne w symulacji | **Podstawowa arytmetyka `f64` (`+ − × ÷`, `sqrt`) jest deterministyczna międzyplatformowo** — IEEE-754 wymaga poprawnego zaokrąglenia, a Rust nie stosuje fast-math ani automatycznej kontrakcji do FMA. Używać jej wolno (z zastrzeżeniem §2: nie w pieniądzu) i nie ma powodu uciekać od niej do fixed-point. Niedeterminizm wnosi wyłącznie **libm w funkcjach przestępnych**, dlatego: **zakaz `f64::ln`/`exp`/`powf`/`tanh`/`atan2` i pokrewnych ze std w kodzie symulacji**; `engine/core` dostarcza `core::det_math` z własnymi implementacjami o udokumentowanej dokładności (≤2 ULP), sumowaniem w kolejności indeksów i bez `mul_add`. Egzekwowane lintem `clippy.toml` + `-D warnings` w CI. Faza potrzebująca nowej funkcji dopisuje ją **do `det_math`** z wektorem testowym, nigdy lokalnie przez `#[allow]`. Dozwolone bez ograniczeń w renderze, UI i audio. Właściciel: M0 | wszystkie fazy z funkcjami użyteczności: M5, M7, M9, M10 |
| K-7 | Cena w ofercie: brutto czy netto | **Cena w `Offer` to zawsze kwota, którą faktycznie płaci kupujący.** Dla detalu (B2C) jest to cena brutto — mieszkaniec porównuje w funkcji użyteczności to, co realnie wyjmie z portfela, i tak też widzi ją w sklepie. Dla hurtu (B2B) jest to cena netto, bo VAT jest dla firmy przelotowy. Rozróżnienie niesie pole `Offer.price_basis: PriceBasis{GrossRetail, NetB2B}`, a nie domysł z kontekstu. VAT wyodrębnia się przy rozliczeniu transakcji do `Transaction.tax` (funkcja `vat_from_gross` M8). Konsekwencja dla gracza jest zamierzona: podwyżka VAT-u uderza w popyt natychmiast, bo zmienia cenę widzianą przez klienta | M5 (właściciel `Offer`), M6, M8, M9 |
| K-8 | Wspólne słowniki domenowe | **Enumy używane przez więcej niż jedną fazę mieszkają w `engine/core`, nie w crate'cie tej fazy, która ich użyła pierwsza.** Dotyczy co najmniej: `UtilityKind` (M5 używa w `TxKind::Utility`, zanim M8 zbuduje sieci), `TransportMode`, `PlaceRef`, `NeedKind`, `ActivityKind`, `DecisionReason` (K-12). Powód jest twardy: bez tego powstaje cykl zależności `sim/agents` → `sim/traffic` → `sim/economy` → `sim/agents`, którego Cargo nie skompiluje, a obejście przez duplikat enuma rozjeżdża się przy pierwszej zmianie. `core` nie dostaje przy tym logiki — wyłącznie słownik i konwersje | M0 (właściciel), M3, M4, M5, M8 |
| K-9 | Strajki i związki zawodowe | Mechanika należy do **M10** (związki, negocjacje, formowanie żądania). M8 dostarcza wyłącznie wyzwalacz zdarzeniowy i skutki miejskie. M7 dostarcza dane o płacach i zyskach. Żadna z tych faz nie implementuje negocjacji po swojemu | M7, M8, M10 |
| K-10 | Upadłość z tytułu zaległości podatkowych | Postępowanie upadłościowe prowadzi **M7** (jest właścicielem `Bankruptcy`). M8 jest tylko wierzycielem zgłaszającym roszczenie — nie ma własnej ścieżki likwidacji firmy | M7, M8 |
| K-11 | Silnik reguł: jeden dla gracza i dla AI | **Korekta macierzy z §1.** PRD §6.3 obiecuje graczowi „ten sam zestaw narzędzi co AI", więc nie mogą istnieć dwa silniki reguł. Powstaje crate **`sim/policy`** (AST, ewaluator, zakresy stosowania), którego właścicielem jest **M7** — bo M7 jest wcześniej i i tak potrzebuje polityk dla tysięcy firm. **M9 dokłada na wierzchu edytor UI, diagnostykę, dry-run i podpowiedzi** oraz pozostaje autorem projektu języka: specyfikacja AST z dokumentu M9 jest wiążąca dla M7, tylko crate zmienia adres z `game::policy` na `sim/policy`. Konsekwencja wymagana przez PRD: reguła gracza i polityka firmy AI wykonują się tym samym kodem, a różnica leży w jakości menedżera, nie w silniku | M7 (właściciel), M9 (język + edytor), M10 |
| K-12 | `DecisionReason` | **Jeden centralny enum w `engine/core`**, bez `#[non_exhaustive]` — brak ramienia renderującego ma łamać kompilację `game/`, a nie po cichu wyświetlać pustą kartę inspekcji. Każda faza dopisuje własne warianty; wyjaśnialność z §7 jest wtedy egzekwowana przez kompilator, a nie przez dobre chęci | wszystkie |
| K-13 | Jednostki i autorytet terenu | **Kontrakt `TerrainQuery` opublikowany przez M1 jest jedynym źródłem prawdy o terenie i jego jednostkach** (wysokość w jednostkach 0,5 m jako `i32`, nachylenie `u8`, współrzędne w metrach). Żadna faza nie wprowadza własnej reprezentacji ani konwersji „u siebie". Faza, której czegoś brakuje w tym interfejsie (np. szerokości przekroju doliny), **zgłasza to do M1 jako rozszerzenie `TerrainQuery`**, a nie liczy tego samodzielnie z surowych danych | M1 (właściciel), M2, M4, M6 |
| K-14 | Geometria pasów: `RoadNetwork` (M2) vs `RoadGraph` (M4) | M2 zapisuje **oś drogi, klasę, liczbę pasów, tonaż i struktury** (most/tunel/nasyp) — czyli to, co wynika z planowania miasta. **M4 wyprowadza z tego geometrię pasów, skrzyżowania i kierunki ruchu** przy budowie `RoadGraph`. M2 nie modeluje pasów, M4 nie modeluje urbanistyki. Granica: jeśli dana jest potrzebna do narysowania miasta — M2; jeśli do przejechania nim — M4 | M2, M4 |
| K-15 | Tydzień w kalendarzu 360-dniowym | **Tydzień 7-dniowy istnieje i dryfuje względem miesiąca.** 30 nie dzieli się przez 7, więc tydzień nie jest podwielokrotnością miesiąca — i dobrze, bo tak samo jest w rzeczywistości. `DayOfWeek` wyprowadza się z absolutnego numeru doby modulo 7 i mieszka w `SimCalendar` (`engine/core`), nie w `game/`. Rytm tygodniowy jest za mocny w detalu, w grafikach zmianowych (PRD §5.5: zmiana weekendowa) i w godzinach handlu, żeby go poświęcić dla elegancji podziału. Konsekwencja przyjęta świadomie: pierwszy dzień roku wypada co roku w inny dzień tygodnia. Agregacja wykresów używa dekad (3 × 10 dni), nie tygodni — dekada dzieli miesiąc bez reszty i sumy są bezstratne | M0 (właściciel `SimCalendar`), M3, M8, M9 |
| K-16 | Partie i oferty poza ECS | **Korekta §2.** Pierwotnie `BatchId` i `OfferId` były `Entity`. To był błąd skali: partii jest rzędu 600 tys., ofert podobnie, obie kategorie mają skrajnie wysoką rotację (powstają i giną w każdym ticku), a **żadna z nich nie jest nigdy odpytywana przekrojowo po archetypach** — do partii dociera się zawsze przez magazyn, pojazd lub półkę, do oferty przez indeks przestrzenny kategorii. Płacenie za nie mutacją strukturalną ECS (bufor komend, przenoszenie archetypu) to koszt bez korzyści. Obie dostają **dedykowane areny z uchwytem `{index: u32, generation: NonZeroU32}`**. Wymagania nienegocjowalne: arena wchodzi do funkcji haszującej stan (§3.6) w kolejności indeksów, uchwyt po zwolnieniu nigdy nie jest ponownie ważny (stąd generacja), a snapshot i zapis traktują arenę jak sekcję ECS. Właściciel mechanizmu areny: M0; instancje: M6 (partie), M5 (oferty) | M0, M5, M6, M12 |
| K-18 | Co zrobić z błędem znalezionym w planie **następnej** fazy | **Poprawka wędruje w przód w tej samej zmianie, w której powstała.** Jeśli praca nad fazą X pokazuje, że dokument fazy Y (Y > X) jest błędny, nieścisły albo niedopowiedziany — poprawiamy dokument Y **teraz**, a nie „jak dojdziemy do Y". Dotyczy pięciu sytuacji, wszystkich zaobserwowanych w praktyce: (1) kontrakt, na którym Y stoi, wygląda inaczej, niż Y zakłada; (2) API, którego Y używa w przykładzie, nie istnieje albo nazywa się inaczej; (3) kryterium ukończenia Y jest niemierzalne albo spełnione tożsamościowo; (4) kolejność pakietów w Y jest niewykonalna, bo któryś potrzebuje danych, które powstają po nim; (5) pakiet Y obiecuje coś, czego żaden pakiet nie jest właścicielem. Zasady: **(a)** poprawka idzie do dokumentu, którego dotyczy — dziennik `00-postep.md` odnotowuje wyłącznie, że powstała; **(b)** każdy dokument zbiera je w tabeli **„Zmiany wpisane po MX"** na swoim końcu, w formacie tabeli korekt (`D-n`, gwiazdka = zmiana zakresu albo kryterium); **(c)** wpisujemy **tylko to, co wiemy na pewno** — faza następna nie jest przy okazji przeprojektowywana; **(d)** poprawka, która wymaga decyzji nie do podjęcia teraz, ląduje w §9.2 dokumentu **fazy** jako decyzja otwarta z propozycją domyślną, nigdy jako komentarz `TODO` w kodzie; **(e)** zmiana dotykająca kontraktu z tego dokumentu nadal wymaga własnego `K-n` — K-18 nie jest obejściem §4a. Uzasadnienie: plan jest wart tyle, ile jest prawdziwy w chwili, gdy ktoś go otwiera. Poprawka odłożona do startu Y kosztuje tyle samo pracy plus ryzyko, że ten, kto zaczyna Y, o niej nie wie — a wiedzę ma ten, kto ją właśnie zdobył, i nikt inny | wszystkie |
| K-5 | Tolerancja LOD | Patrz §4 — mikro↔mezo tolerancja 0, makro z kontraktem słabszym, ale z zachowaniem pieniądza i masy co do grosza i grama | M4, M10, M12 |
| K-19 | Katalog `data/ui/` | **Dopisany do §5.** Nakładki danych na terenie (`TerrainOverlay` z M1) potrzebują palety, progów i jednostki, a te są danymi, nie kodem: klient graficzny i podgląd `headless` mają rysować **tę samą** mapę, więc nie mogą mieć dwóch tabel barw. M2 tworzy `data/ui/overlays.ron` z jednym wpisem (`land_value`), każda następna faza dopisuje swój. Nazwa wyświetlana siedzi pod `loc_key` i podłączy się do `data/locale/`, kiedy `engine/ui` powstanie w M3 — do tego czasu klient wypisuje ją z tabeli zapasowej w kodzie. Lista katalogów w §5 deklaruje się jako kompletna, więc brak wpisu był jej błędem, nie luką | M2 (właściciel), M8, M9, M10, M11 |
| K-20 | Słowniki, które M3 wnosi do `engine/core` | **Rozszerzenie listy z K-8 o cztery pozycje, wpisane po M3a.** `core` dostaje: **`PlaceKind`** (rodzaj miejsca zaspokajającego potrzebę — M3 wybiera cel, M5 stawia tam sklep z ofertą, M8 instytucję z pojemnością), **`TraitId`** (indeks cechy w `Personality` — M3 planuje czas wolny, M5 czyta `PriceSensitivity` i `Loyalty` w §6.4, M7 `Ambition` przy zmianie pracy), **`DeprivationEffect`** (ładunek `DecisionReason::Deprivation`; ładunek centralnego enuma **musi** mieszkać w `core`, inaczej `core` zależałby od `sim/agents`) i **`MinuteOfDay`** (minuta doby 0..1439 — wymieniają się nią planer M3, podróże M4 i godziny handlu M5). Przy okazji: **`NeedKind` dostaje dwanaście wariantów z M3a §5.5 w miejsce dziesięciu wpisanych roboczo w M0** — kolejność jest kontraktem, bo `Needs.level: [u8; 12]` indeksuje się `as_index()`; nic tych dziesięciu jeszcze nie używało. Do `core` przenosi się też **`assets::{data_dir, data_path}`** z `sim/world` (katalog `data/` jest kontraktem globalnym z §5, a `sim/agents` nie może zależeć od `sim/world` — zależność idzie w drugą stronę); `magnat_world` re-eksportuje, więc nazwy z M1 §6 zostają. **Uzupełnienie po M3b:** dochodzą **`CommitmentKind`** (rodzaj zobowiązania w planie dnia — ładunek `DecisionReason::Commitment`; M4 czyta przy dojazdach, M7 przy grafikach zmianowych) i **`StockCat`** z `STOCK_CAT_COUNT` oraz `StockCat::need()` (kategoria zapasu gospodarstwa — ładunek `DecisionReason::StockBelowThreshold`, indeks `Household.stock` w M3c, odwzorowanie na `GoodId` w M5). Oba spełniają to samo kryterium co poprzednie cztery: ładunek centralnego enuma **nie może** pochodzić z crate'u, który od `core` zależy — ta sama reguła, która wypchnęła `ReplanCause` z `DecisionReason` do `sim/agents`. **Uzupełnienie po M3c:** dochodzą **`MigrationKind`** (kierunek i powód decyzji migracyjnej gospodarstwa — ładunek `DecisionReason::MigrationDecision`; M8 czyta przy polityce mieszkaniowej, M10 w historii „na sucho") i **`LifeEventKind`** (zdarzenie cyklu życia — ładunek `DecisionReason::LifeEvent`; M7 czyta `Retired`, M8 `Died` i `FellIll`). Blok `DecisionReason` fazy M3 zamyka się tym na **100–119** | M0 (właściciel `core`), M3 (wnosi), M4, M5, M7, M8 |
| K-21 | System, który jest funkcją nad całym światem | **`engine/ecs` dostaje system wyłączny**: `SystemDesc::exclusive()` znaczy „konfliktuje z każdym innym systemem", więc scheduler stawia go **sam na jego poziomie** i domyka nim etap, a `SystemCtx::world_mut()` daje mu `&mut World`. Potrzebne tam, gdzie krok symulacji jest gotową funkcją nad całym światem i nie da się go opisać zbiorem komponentów — w M3 dotyczy to pętli doby (`DayLoopSystem`) i rytmu społeczeństwa (`SocietySystem`, opakowanie `society::step_day`). Rozpisanie `demography::step_day` na deklaracje dostępu nie kupiłoby ani jednej krawędzi DAG, bo dotyka wszystkiego o mieszkańcu; dałoby za to **fałszywą** deklarację, gdyby ktoś czegoś nie wypisał. Determinizm jest przy tym trywialny (00 §3.3): system wyłączny nie ma z kim się ścigać. Cena jest jawna i zamierzona — taki system nie korzysta ze zrównoleglenia, więc wolno go użyć **tylko** wtedy, gdy podział na dostępy nic nie kupuje. Właściciel mechanizmu: M0 | M0 (właściciel `ecs`), M3, M7, M8, M10 |
| K-22 | Zegar gry | **`SimClock` i `SimSpeed` mieszkają w `engine/core`** (wykonanie decyzji 9.1 z M3). Zegar jest **przelicznikiem po stronie prezentacji**, nie stanem symulacji: zamienia miniony czas realny na **liczbę minut gry do wykonania** i nie wchodzi do hasha stanu (00 §3.6). Kod symulacji go nie widzi — widziałby wtedy czas rzeczywisty, czego zabrania §3.5. Stąd bierze się wymóg PRD §14.5 „prędkość nie wpływa na wynik": każda minuta jest tym samym tickiem niezależnie od tego, ile ich wypadło na sekundę. Prędkości: `Paused`, `X1` (1 minuta gry na sekundę realną), `X3`, `X10`; **50× dopisuje M12 na końcu enuma**, bo dyskryminanty wchodzą do zapisu. `engine/ui` dostarcza wyłącznie widget | M0 (właściciel), M3 (widget), M9, M11, M12 |
| K-23 | Gdzie mieszka `RoadClass` | **Wykonanie `K-8` dla klasy drogi: enum przenosi się z `sim/world` do `engine/core::vocab`.** Wpisane przy starcie M4a. Powód jest twardy, nie estetyczny: `engine/nav` (M4) musi znać klasę krawędzi, a **nie może zależeć od `sim/world`**, bo `sim/world` zależy od `sim/agents`, a `sim/agents` od M4b zależy od `magnat-nav` (`Z-1` w dokumencie M4) — zależność w drugą stronę zamknęłaby cykl, którego Cargo nie zbuduje. Duplikat enuma po stronie M4 odpada z tego samego powodu co zawsze (`K-8`): rozjeżdża się przy pierwszej zmianie, a tu kolejność wariantów jest kontraktem, bo indeksuje tablicę `SPECS`. **Co się przenosi:** `RoadClass` z metodami czystymi (`rank`, `is_driveable`, `is_rail`, `key`) plus to, co daje makro `vocab_enum!` (`ALL`, `as_index`, `from_index`, `name`). **Co zostaje w `sim/world`:** `ClassSpec` i tablica `SPECS` — to parametry generacji miasta (szerokość pasa, długość odcinka, limity mostu i tunelu), a nie słownik; wraz z nimi zostają `spec(class)` i `forbids_heavy(class)`, które z metod stają się funkcjami wolnymi (reguła sieroty), a `city::road` reeksportuje `RoadClass`, więc nazwy z M2 §6 nie drgnęły. M4 nie potrzebuje `ClassSpec` ani razu: prędkość, tonaż i liczba pasów są już w `RoadSegment` | M0 (właściciel `core`), M2 (oddaje), M4, M8, M11 |
| K-24 | Katalogi `data/vehicles/` i `data/roads/` | **Dopisane do §5.** Ruch potrzebuje dwóch tabel, których nie ma gdzie indziej: **`data/vehicles/classes.ron`** (klasy pojazdów — masa własna, zbiornik, zużycie odniesienia, mnożniki wzoru paliwowego i cena paliwa) oraz **`data/roads/vdf.ron`** (diagram podstawowy: przepustowość pasa, gęstość krytyczna, prędkość przy zapełnieniu, odstęp korkowy). Obie są **danymi, nie kodem**, i to jest warunek działania `balansator calibrate-vdf` z M4d: kalibracja przepisuje tabelę VDF i wynik ma być widoczny w diffie, a nie w rekompilacji. Kolejność wpisów w `classes.ron` jest kontraktem, bo `VehicleClassId` to indeks w tej tablicy i siedzi w komponencie pojazdu, czyli w zapisie gry. Lista katalogów w §5 deklaruje się jako kompletna, więc brak wpisu byłby jej błędem, nie luką | M4 (właściciel), M6 (ciężarówki jako klasy pojazdów), M8 (podatek od pojazdów), M12 (modding) |
| K-25 | Jednostka paliwa w symulacji | **Wewnątrz ruchu paliwo liczy się w mikrolitrach**, a `Volume` (mililitry, §2) pojawia się dopiero na granicy modułu (`FuelTank::volume`, zdarzenie `FuelPurchased`, karta inspekcji). Powód jest arytmetyczny i zmierzony: krawędź metropolii M2 ma średnio ~40 m, zużycie na niej to ~2,5 ml, a `settle_edge` wywołuje się rzędu ośmiu razy na pojazd na minutę. W mililitrach każde takie wywołanie gubi kilkanaście procent na zaokrągleniu **w jedną stronę**, więc błąd nie znosi się, tylko kumuluje w każdej podróży. To nie jest odstępstwo od §2 — §2 mówi, w czym wyraża się `Volume`, a nie w czym wolno liczyć pośrednio; zakaz dotyczy floata, nie drobniejszej działki całkowitej. Katalog `data/vehicles/` podaje pojemności w mililitrach, bo tak je czyta człowiek; przeliczenie jest w jednym miejscu (`FuelTank::full`) | M4 (właściciel), M5 (obrót stacji), M6 (zbiorniki i dostawy), M8 (akcyza) |
| K-26 | Pogoda przed M8 | **Zaślepka mieszka w `engine/core` jako `weather_at(seed, day) -> Weather`, a nie w fazie, która jej pierwsza potrzebuje** (wykonanie decyzji `D4` z M4). Powód jest ten sam co przy `K-8`: pogody używa M4 (dyskomfort roweru i pieszego w wyborze środka), M5 (sezonowość popytu), M8 (zdarzenia miejskie) i M10 (historia „na sucho"), a `core` jest jedynym miejscem, od którego wszystkie one zależą. Model jest **całkowitoliczbowy i bez funkcji przestępnych** (`K-6`): trójkąt roczny temperatury, opad losowany strumieniem `StreamId::WeatherStub` z kluczem doby, zero `sin`. Czysta funkcja `(seed, day)` — ta sama doba daje tę samą pogodę niezależnie od kolejności odpytania, więc nie jest stanem i nie wchodzi do hasha. **M8 podmienia ciało, nie sygnaturę**, i wtedy dopiero pogoda staje się stanem z własnym hakiem hasha; do tego czasu żadna faza nie ma prawa trzymać własnej. Numer strumienia zostaje przy M4, bo to jego zapisy niosą go pierwsze. **Wykonane w M8c, z jednym doprecyzowaniem:** pogoda jest od tej chwili stanem (`magnat_events::WeatherState` — procesy z pamięcią dla temperatury i wilgotności, pierścień opadu na 90 dób, pokrywa śnieżna) i wchodzi do hasha przez zasób `Events`. Zaślepka `weather_at` **zostaje w `core` i dalej działa** — świat bez `sim/events` (scenariusze M3 i M4) straciłby inaczej sezonowość, a `K-26` obiecywał podmianę ciała, nie odebranie funkcji. Rozstrzyga o tym `TrafficOracle`: pierwsze wywołanie `set_weather` czyni pogodę zewnętrzną wyłączną i od tej chwili zaślepka w tym świecie milczy, więc dwóch pogód naraz nie ma nigdy. Proces **ma pamięć**, choć funkcja czysta byłaby tańsza, i to jest decyzja: przy losowaniu niezależnym doba po dobie suma opadu z trzydziestu dób trzyma się średniej tak ciasno, że sonda niedoboru nigdy nie doszłaby do wartości, przy których krzywa suszy zaczyna rosnąć — czyli susza byłaby zdarzeniem martwym (`R2`) | M0 (właściciel `core`), M4 (wnosi), M5, M8 (przejmuje), M10 |
| K-27 | Katalog `data/roads/` — zakres | **Rozszerzenie `K-24`.** Katalog niesie **parametry ruchu**, nie tylko diagram podstawowy: obok `vdf.ron` mieszka w nim `mode_choice.ron` (wartość czasu, mnożniki celu, składniki dyskomfortu, progi wykonalności, taryfy i **widełki rozkładu udziału środków transportu**). Powód jest ten sam, dla którego VDF jest daną: `balansator` ma przestawiać udział środków transportu bez rekompilacji, a wynik ma być widoczny w diffie. Nowy katalog `data/travel/` byłby czystszy nazwą i gorszy praktyką — dwa katalogi na jedną fazę i jeden moduł. **Widełki rozkładu są przy tym kryterium akceptacyjnym, a nie kalibracją**: PRD §20.1 mówi „w zakresach referencyjnych dla epoki" i liczb nie podaje, więc podaje je ta tabela i ona jest bramką scenariusza odniesienia | M4 (właściciel), M8 (regulacje ruchu), M12 (modding) |
| K-28 | Pule imion i nazwisk per region | **Kolejność regionów w `data/names/regions.ron` i kolejność wpisów w plikach `first_names_<kod>.ron` oraz `surnames_<kod>.ron` są kontraktem zapisu gry**, tak samo jak kolejność klas w `data/vehicles/classes.ron` (`K-24`). Powód jest arytmetyczny: `Identity.first_name` i `last_name` to indeksy w **jednej tablicy sklejonej ze wszystkich regionów w tej kolejności**, a nie w tablicy jednego regionu — dzięki temu sam indeks niesie region i płeć, a komponent nie rośnie o ani jedno pole i zostaje przy 16 bajtach. Cena: przestawienie kolejności albo usunięcie regionu przenumerowuje **wszystkich** mieszkańców każdego zapisanego świata. Dopisywać wolno wyłącznie na końcu — regionu na końcu `regions.ron`, imienia na końcu swojej listy, nazwiska na końcu `entries`. Nazwisko jest **parą form** `(męska, żeńska)` w danych, a nie regułą sufiksową: „Kowalski → Kowalska", ale „Nowak" i „Schmidt" mają jedną formę dla obu płci, a lista wyjątków do reguły byłaby dłuższa od listy nazwisk. Pule **nie są lokalizacją UI** (CLAUDE.md) i nie mają odpowiednika w `data/locale/`. Właścicielem katalogu jest M4 (wniósł go jako dług `Z-7` po M3); walidator modów z M12 sprawdza kolejność tak samo jak przy pojazdach | M4 (właściciel), M10 (kroniki i rody), M12 (modding, lokalizacja) |
| K-29 | Arena, do której sięga system spoza ECS | **Uzupełnienie `K-16`, wpisane po M5b.** `K-16` wymaga, żeby arena wchodziła do funkcji haszującej stan **w kolejności indeksów**, i to wymaganie zostaje bez zmian. Nie przesądza natomiast, **którą sekcją hasha** arena wchodzi, a `World::register_arena_hash::<T>(kind)` z M0 umie czytać wyłącznie `Arena<T>` **stojącą jako osobny zasób świata**. To za mało dla areny, po którą sięga kod niemający `&World`: `sim::agents::PlaceProvider` (punkt podmiany `Z-1`) dostaje `&self`, bo zasób `AgentSources` jest na czas minuty wyjmowany z ECS, żeby pętla doby mogła trzymać `&mut World` obok. Arena ofert M5 musi więc mieszkać **wewnątrz** struktury współdzielonej uchwytem (`Market(Arc<Mutex<…>>)`), tak samo jak `TrafficOracle` w M4. **Rozstrzygnięcie: arenę wolno trzymać wewnątrz zasobu**, jeśli ten zasób haszuje ją przez `Arena::hash_state` (czyli w kolejności indeksów, ze slotami i generacjami) i sam jest wpięty przez `register_resource_hash`. Sekcja hasha zmienia się wtedy z „areny wg `ArenaKind`" na „zasoby wg nazwy typu" — i to jest cała różnica. `ArenaKind::Offers` zostaje zarezerwowane i niezmienne, bo wartości `ArenaKind` są wieczne tak samo jak `StreamId`. M6 (partie) decyduje sam: jeśli `sim/supply` nie ma tego ograniczenia, korzysta z `register_arena_hash` bez zmian | M0 (właściciel mechanizmu), M5 (pierwszy przypadek), M6, M12 (zapis i migracje) |
| K-30 | Słowniki, które M5 wnosi do `engine/core` | **Wykonanie `K-8` dla M5, wpisane po M5c.** `core` dostaje **`PriceDriver`** (który człon korekty przeważył przy zmianie ceny — ładunek `DecisionReason::Repricing`; czyta go `engine/ui` w karcie inspekcji i panel sklepu M5e, a M7 przy politykach cenowych firm AI). Kryterium jest to samo co przy `K-20`: **ładunek centralnego enuma nie może pochodzić z crate'u, który od `core` zależy**, a `UtilityKind` się nie nadaje, bo opisuje wymiar oceny **kupującego**, nie człon korekty **sprzedawcy** — wciśnięcie tam `Stock` i `Spoilage` zepsułoby kartę mieszkańca po stronie M3. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::Repricing = 303` mieści się w bloku M5 (300–399, `K-12`), a `StreamId::FirmPricing = 186` w bloku M5 (180–199, `K-4`) — obie wartości są od tej chwili wieczne. Kolejność wariantów `PriceDriver` jest kontraktem, bo `as_index()` indeksuje histogram powodów przecen w panelu | M0 (właściciel `core`), M5 (wnosi), M7, M9 |
| K-31 | Słowniki, które M5 wnosi do `engine/core` — uzupełnienie po M5d | **Kontynuacja `K-30`.** `core` dostaje trzy kolejne słowniki, wszystkie z tego samego powodu co `PriceDriver`: są **ładunkami centralnego enuma** `DecisionReason`, a ładunek nie może pochodzić z crate'u, który od `core` zależy. **`LoanKind`** (`Consumer`, `WorkingCapital` — ładunek `CreditApproved`/`CreditRejected`; M7 dokłada inwestycyjny, M10 hipoteczny **na końcu**, bo kolejność indeksuje widełki w `data/economy/bank.ron`), **`RejectCredit`** (`NoIncome`, `DstiTooHigh`, `Arrears`, `DscrTooLow`, `NoLender` — osobny słownik od `RejectCause` z rozmysłu: tamten indeksuje histogram **utraconych sprzedaży** i jego kolejność jest kontraktem panelu sklepu, więc wspólny enum kosztowałby dwa martwe warianty po każdej stronie) oraz **`FixedCost`** (`Housing`, `Utilities`, `Insurance`, `LoanService` — ładunek `BudgetShortfall`, kolejność indeksuje `HouseholdBudget.fixed`). Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{CreditApproved = 304, CreditRejected = 305, BudgetShortfall = 306}` mieszczą się w bloku M5 (300–399, `K-12`), a `StreamId::CreditScoringJitter = 185` był zarezerwowany w bloku M5 od M5a. Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M5 (wnosi), M7, M8, M9, M10 |
| K-33 | Gdzie mieszka `GateKind` | **Wykonanie `K-8` dla rodzaju bramy: enum przenosi się z `sim/world` do `engine/core::vocab`.** Wpisane przy starcie M6a. Powód jest ten sam co przy `RoadClass` (`K-23`) i równie twardy: `Good::import_via` jest polem katalogu towarów, katalog należy od M6a do `sim/supply`, a `sim/supply` **nie może zależeć od `sim/world`** — zależność idzie w drugą stronę, bo to generator miasta pyta katalog przy domykaniu Etapu 7, a nie katalog generator. Duplikat enuma odpada z tego samego powodu co zawsze: rozjeżdża się przy pierwszej zmianie, a tu kolejność wariantów jest kolejnością przetwarzania w generatorze. **Co się przenosi:** `GateKind` z metodami czystymi (`key`, `is_rail`, `capacity`) plus to, co daje `vocab_enum!`. **Co zostaje w `sim/world`:** `CityGate` i cała geometria bram — to jest urbanistyka, nie słownik; `city::gates` reeksportuje `GateKind`, więc nazwy z M2 nie drgnęły | M0 (właściciel `core`), M2 (oddaje), M6, M8 |
| K-34 | Słowniki, które M6 wnosi do `engine/core` | **Wykonanie `K-8` dla M6, wpisane po M6a.** `core` dostaje dwa: **`LossKind`** (`Drying`, `Evaporation`, `Spoilage`, `Expired`, `Spillage`, `ProcessWaste`, `Setup`, `TransportDamage`, `Theft`, `Storage` — powód, dla którego masa zeszła z bilansu) i **`NeedCategoryId`** (indeks w `data/needs/categories.ron`). Kryterium jest to samo co przy `K-30` i `K-31`: więcej niż jedna faza. `LossKind` księguje M5 (odpis towaru przeterminowanego na półce) i M7 (odpis w rachunku wyniku), a kolejność wariantów jest kontraktem, bo `as_index()` indeksuje histogram strat. `NeedCategoryId` czyta M5 przy substytucji i M7 przy wypełnianiu fali B katalogu. **Uwaga do `NeedCategoryId`, bo wygląda podobnie do czegoś, co już jest:** to **nie** jest `StockCat`. `StockCat` ma osiem wariantów i indeksuje `Household.stock: [u8; 8]` — jest grubym podziałem spiżarni gospodarstwa. `NeedCategoryId` jest drobny („pieczywo", „paliwo silnikowe", „części maszyn") i odpowiada na pytanie „czym to podmienić" z PRD §8.4. Jedna kategoria potrzeby wpada w co najwyżej jeden `StockCat`, a odwzorowanie mieszka **w danych** (`NeedCategory::stock_cat`), nie w kodzie, bo to kwestia zawartości katalogu, nie typu. Kategorie bez odpowiednika (ruda żelaza, olej smarowy) mają `None` i nie udają, że gospodarstwo je trzyma. **Uzupełnienie po M6b:** dochodzą trzy pozycje, wszystkie z tego samego kryterium. **`LineStopCause`** (dlaczego linia nie produkuje — ładunek `DecisionReason::ProductionHalted`; czyta go karta inspekcji zakładu, pulpit firmy M7 i alert miejski M8 przy odcięciu mediów; `Starved` i `Blocked` siedzą tam obok awarii z rozmysłu, bo dla gracza „stoi, bo nie ma mąki" i „stoi, bo się zepsuło" to ta sama klasa pytania). **`ShortageStageKind`** (stopień kaskady niedoboru **bez ładunku** — ładunek zostaje po stronie M6 w `supply::ShortageStage`; kolejność wariantów jest kolejnością prób z PRD §8.4 i jest kontraktem, bo test kaskady sprawdza, że przejścia idą po `as_index()` w górę). Oraz — **nie enum, lecz struktura** — **`OpenHours` przenosi się z `sim/agents::places` do `engine/core::time`**: pyta o nią M3 przy planowaniu doby, M6 przy godzinach dostaw na rampie (§5.5) i M8 przy regulacji miejskiej, a „czynne przez północ" jest warunkiem brzegowym, którego nikt nie chce pisać dwa razy. `sim/agents::places` reeksportuje, więc nazwy z M3 nie drgnęły; `default_hours(PlaceKind)` **zostaje** w M3, bo to polityka, nie słownik. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{Shortage = 400, ProductionHalted = 401, SubstituteUsed = 402}` otwierają blok M6 (400–499, `K-12`), a `StreamId::{SupplyBreakdown = 200, SupplyTransitDelay = 205}` blok M6 strumieni (200–219, `K-4`). Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M6 (wnosi), M3 (oddaje `OpenHours`), M5, M7, M8 |
| K-35 | Katalog `data/tuning/` | **Dopisany do §5.** Progi kaskady niedoboru, taryfy mediów, zużycie maszyn i stawki transportowe są **kalibracją, nie projektem**: mają się zmieniać przebiegiem balansatora i być widoczne w diffie, a nie w rekompilacji. To ten sam argument, który wypchnął diagram podstawowy ruchu do `data/roads/vdf.ron` (`K-24`), i wykonanie decyzji otwartej `D7` fazy M6. Pierwszym plikiem jest `data/tuning/supply.ron` (właściciel M6), w którym siedzi też `EMISSION_REF_PM_G_PER_MIN` zapowiedziany w M6 §6.4.3 — stała odniesienia skali emisji **musi** być stała, a nie „najbrudniejszy zakład epoki", bo ruchomy mianownik znaczyłby, że ten sam komin dymi inaczej po wejściu nowej epoki. Katalog jest **wspólny dla faz**, nie prywatny dla M6: strojenie M7 (progi upadłości), M8 (stawki opłat) i M10 (tempo R&D) należy tu, a nie do katalogów dziedzinowych — `data/economy/` jest własnością M5 i nie ma być workiem na cudze liczby. Rozróżnienie, które trzeba trzymać: w `data/tuning/` siedzą liczby, **które wolno przestawić bez zmiany znaczenia modelu**; parametry zmieniające kształt rozwiązania zostają w katalogach dziedzinowych. Lista katalogów w §5 deklaruje się jako kompletna, więc brak wpisu byłby jej błędem, nie luką | M6 (właściciel katalogu), M5 (balansator stroi), M7, M8, M10, M12 (modding) |
| K-36 | Oferta B2B: widok na `Offer` czy własny typ | **Własny typ `supply::b2b::Quote`. Korekta założenia `K-7`, nie jego treści.** §5.8 fazy M6 mówiło „`Quote` jest widokiem na `Offer` z `price_basis: NetB2B`; M6 nie tworzy drugiego typu oferty" — i w tym samym akapicie wypisywało `struct Quote` z dziewięcioma własnymi polami. Sprzeczność rozstrzyga się na korzyść struktury, z trzech powodów, z których każdy osobno wystarcza. **(1) Kierunek zależności:** `Offer` mieszka w `sim/economy`, a rynek B2B potrzebuje magazynu, zakładu, trasy i katalogu, czyli `sim/supply`; `sim/economy` zależy od `sim/supply` i **nigdy odwrotnie**, więc `Quote` po stronie ekonomii sięgałby po cudze wnętrze przy każdej wycenie. **(2) Indeks:** `OfferIndex` M5 warstwuje się po `CategoryId::Stock(StockCat)` — osiem warstw spiżarni gospodarstwa. Ruda żelaza, olej smarowy i ropa **nie mają `StockCat`**, więc oferta B2B nie ma warstwy, w której mogłaby stanąć; przewarstwowanie indeksu po `GoodId` to czterysta siatek przestrzennych na rzecz zapytań, które i tak nie są przestrzenne. **(3) Kształt:** `Offer` jest **stojącą** ceną półkową (`unit_price`, `available`, `price_rev`), `Quote` jest **odpowiedzią na jedno zapytanie** (`mass_available`, `ready_at`, `eta`, `valid_until`, `freight_minutes`) i umiera razem z nim. Dołożenie pięciu pól B2B do 56-bajtowej struktury trzymanej w arenie setek tysięcy ofert detalicznych byłoby kosztem płaconym przez M5 za wygodę M6. **Co z `K-7` zostaje bez zmian, i to jest istota:** cena to zawsze kwota płacona przez kupującego, cały rynek B2B liczy **netto**, rozróżnienie niesie **jawna deklaracja, a nie domysł z kontekstu**, a cła i akcyza doliczają się osobno przez `ChargeRegistry` M8, nigdy w środku ceny. `PriceBasis::NetB2B` zostaje w `sim/economy` i dostaje pierwszego czytelnika w WP11, kiedy półka sklepu zacznie kupować u hurtownika | M5 (właściciel `Offer`), M6 (właściciel `Quote`), M7, M8 |
| K-37 | Katalog `data/trade/` | **Dopisany do §5.** Obrót zagraniczny potrzebuje tabeli, która nie należy do żadnego istniejącego katalogu: klasy taryfowe i ich stawki. Nie `data/economy/` — ten jest własnością M5 i nie ma być workiem na cudze liczby (`K-35`); nie `data/tuning/` — tam siedzą liczby, **które wolno przestawić bez zmiany znaczenia modelu**, a klasa taryfowa zmienia przyporządkowanie towaru, czyli kształt. Pierwszym i na razie jedynym plikiem jest `data/trade/tariffs.ron` (właściciel M6, **przejmuje M8**): lista klas, każda z listą domen kluczy towarów (`AD-12`) i stawką `duty_bp`. Przypisanie przez **domenę**, a nie przez tabelę czterystu wierszy — domen jest dwanaście i lista jest zamknięta, więc każdy towar ma klasę z definicji i nowy towar nie może jej zgubić przez przeoczenie w danych; pokrycia pilnuje test. Stawki są **stubem** i mają nim zostać: cło jest polityką miasta i epoki, więc M8 podmienia całą tabelę, nie jej kształt. Lista katalogów w §5 deklaruje się jako kompletna, więc brak wpisu byłby jej błędem, nie luką | M6 (właściciel), M8 (przejmuje stawki), M12 (modding) |
| K-38 | Słowniki, które M6 wnosi do `engine/core` — uzupełnienie po M6c | **Domknięcie `K-34`.** `core` dostaje **`TariffClassId`** (indeks klasy taryfowej w `data/trade/tariffs.ron`, `K-37`). Kryterium to samo co przy `PriceDriver` i `NeedCategoryId`: **dwóch konsumentów znanych z nazwy i numeru fazy** — M6 nadaje klasę w `TradeGood` i przekazuje ją dalej, M8 odczytuje ją przy naliczaniu cła w `ChargeRegistry`. Klasa jest **etykietą, nie stawką**; stawka jest polityką M8 i dlatego cło nie modyfikuje ceny w ofercie (`K-7`, `K-36`): `ImportQuote` niesie cenę netto i osobno rozpisane obciążenie. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{SupplierChosen = 403, ContractSigned = 404, ExportChosen = 405}` domykają blok M6 zapowiedziany komentarzem w `decision.rs`, a `StreamId::{SupplyQuoteNoise = 203, SupplyImportLead = 206}` zajmują numery zarezerwowane imiennie w §6.1 dokumentu M6. Wszystkie te wartości są od tej chwili wieczne. **Czego M6c do `core` nie wnosi i dlaczego:** `TradeNodeKind` z §5.9 **nie powstaje** — rodzajem węzła granicznego jest `GateKind`, który `K-33` przeniósł do `core` dokładnie po to, żeby `Good::import_via` i węzeł mówiły o tym samym; drugi enum rozjechałby się przy pierwszej zmianie i unieważnił `import_via` w całym katalogu fali A. Głowica rurociągu z §5.9 odpada razem z nim: w fali A żaden rurociąg nie przechodzi granicy, a `Carrier::Pipeline` z M6b wozi ropę **wewnątrz** miasta | M0 (właściciel `core`), M6 (wnosi), M8, M9 |
| K-39 | Gdzie mieszka `DepositId` | **Wykonanie `K-8` dla identyfikatora złoża: newtype przenosi się z `sim/world` do `engine/core`.** Wpisane przy starcie M6d. Powód jest mocniejszy niż zwykle, bo do M6c istniały **dwa** typy o tej nazwie i bez konwersji między nimi: `sim/world::DepositId(u32)` (indeks w tablicy złóż świata) i `sim/supply::DepositId(u16)` (pole `BatchOrigin::deposit`). Panel „od pola do półki" z PRD §14.4 obiecuje ślad prowadzący **do konkretnej żyły**, a prowadził do liczby, która przypadkiem wyglądała podobnie — i nic tego nie łapało, bo oba typy kompilowały się osobno. Konsumenci: M1 generuje złoża i prowadzi ich bilans masy (`Deposit::extract` — jedyne miejsce, w którym `extracted` rośnie), M6 przenosi identyfikator przez cały łańcuch. Szerokość `u32`, bo metropolia ma ~6 tys. złóż i zapas w `u16` byłby ciasny bez powodu. `sim/world` re-eksportuje, więc nazwy z M1 §6 nie drgnęły | M0 (właściciel `core`), M1 (oddaje), M6, M9 (panel), M11 (wyrobisko) |
| K-40 | Gdzie mieszka `BodyType` | **Wykonanie `K-8` dla nadwozia pojazdu dostawczego: enum przenosi się z `sim/supply` do `engine/core::vocab`.** Wpisane przy AG-1 w M6d. Dwóch konsumentów znanych z nazwy i numeru fazy: **M4** deklaruje nadwozie przy klasie pojazdu w `data/vehicles/classes.ron`, **M6** stawia je jako wymaganie zlecenia transportowego (`VehicleRequirements::body`). Bez wspólnego słownika wtyczka towarowa M4 nie miałaby czym odpowiedzieć na pytanie „czy ta ciężarówka uwiezie mleko", a duplikat rozjechałby się przy pierwszej zmianie — kolejność wariantów jest kontraktem zapisu gry, bo `BodyType` siedzi w wymaganiach zlecenia, a zlecenia przeżywają zapis. **Co zostaje w `sim/supply`:** `body_for_storage(StorageClass) -> BodyType` jako funkcja wolna (reguła sieroty), bo odwzorowanie klasy przechowywania na nadwozie jest wiedzą o **towarze**, a nie o pojeździe | M0 (właściciel `core`), M4 (katalog pojazdów), M6 (wymagania zlecenia), M8 (regulacje ruchu) |
| K-41 | Gdzie mieszka bilans wydobycia | **Wyczerpanie złoża dzieli się na dwie liczby o dwóch właścicielach, a tablica złóż się nie przeprowadza.** Wpisane po M6e jako rozstrzygnięcie `AO-6`. `Deposit::extracted` (w `WorldData`, M1) znaczy od tej chwili **wyczerpanie sprzed startu świata** — parametr losowany przy generacji, który po jej zamknięciu nie drga i wchodzi do hasha **terenu** razem z geometrią formacji. `DepositLedger` (też M1, ale **posiadający**, nie pożyczający) prowadzi wydobycie **w tej grze**, ma jednego pisarza (`Deposits::extract`) i wchodzi do hasha **stanu** przez `ChainHandle`. To nie są dwie prawdy o tej samej liczbie: pierwsza jest historią świata, druga jego rozgrywką, a `Deposit::remaining()` odpowiada na „ile było w chwili zero”, nie na „ile jest teraz”. **Dlaczego nie jedna tablica pod zamkiem:** `TerrainQuery::deposit(id)` zwraca **referencję** i jest ścieżką zapytań generacji, a kopalnia pisze co minutę — wspólny `RwLock` kosztowałby każde zapytanie terenowe, a druga kopia tablicy byłaby dokładnie tym błędem, który `K-39` właśnie usunął. **Konsekwencja wiążąca:** port `magnat_supply::Deposits` dostaje supertrait `HashState`, bo wydobycie jest stanem symulacji i milczenie hasha o nim znaczyłoby, że dwa przebiegi tego samego ziarna wolno rozjechać w jedynej liczbie, którą kopalnia zmienia. Uchwyt do bilansu niesie `CityData` — tam, gdzie `catalog`, i z tego samego powodu: czyta go most stawiający gospodarkę, a most widzi miasto, nie teren | M1 (właściciel `Deposit` i `DepositLedger`), M6 (czyta przez port), M9 (panel), M11 (wyrobisko) |
| K-42 | Jawna kolejność systemów wobec konfliktu dostępów | **Ograniczenie jawne (`SystemDesc::before`/`after`) ma pierwszeństwo przed krawędzią wyprowadzoną z konfliktu dostępów.** Wpisane po M6e; poprawka w `ScheduleBuilder::build` (`engine/ecs`, crate M0). Konflikt dostępów niesie informację „tych dwóch nie wolno puścić równolegle” i **nie ma zdania o kierunku** — kierunek brał się z kolejności kanonicznej, czyli z `SystemId`, czyli z hasha nazwy systemu. Dla pary systemów **wyłącznych** (`K-21`) konflikt zachodzi z definicji zawsze, więc jawne `before` między nimi kończyło się cyklem zawsze wtedy, gdy hash trafił odwrotnie — a trafiał losowo, bo nazwa jest nazwą. Skutkiem było to, że kontraktu kolejności **nie dało się wyrazić**: `D12` fazy M6 („`SpoilageSystem` przed `RetailSystem`”) musiał do M6d żyć jako komentarz w środku cudzego systemu, a istniejące `economy.Market.before(agents.DayLoop)` działało wyłącznie przez zbieg okoliczności w hashu. Po poprawce krawędź konfliktu jest pomijana dla pary, którą deklaracja ustawiła w drugą stronę — kolejność i tak zostaje wymuszona, tylko kierunkiem, który zadeklarował autor. Determinizm nie zmienia się w niczym: kolejność kanoniczna po `SystemId` zostaje, a `Schedule::fingerprint` dalej jest funkcją zbioru systemów i ich ograniczeń | M0 (właściciel `ecs`), M3, M5, M6, M7, M8, M10 — każda faza deklarująca kolejność semantyczną |
| K-47 | Co M7 wnosi do `engine/core` przy silniku reguł | **Wykonanie `K-8` dla M7c, wpisane po M7c.** Trzy pozycje, wszystkie z jednego powodu: język reguł mieszka w `sim/policy`, a `sim/policy` **nie może zależeć od `sim/economy`** — zależność idzie `economy → firms → policy` i odwrócenie zamknęłoby cykl, którego Cargo nie zbuduje. **(1) `PriceBasis` przenosi się z `sim/economy::offer` do `core::vocab`** (ten sam ruch co `RoadClass` w `K-23` i `GateKind` w `K-33`): `Metric::Price` **musi** nieść podstawę, bo walidator odrzuca mieszanie brutto z netto w jednym porównaniu (`K-7`, M9d §5.6), a duplikat enuma rozjechałby się przy pierwszej zmianie — tu chodzi o liczbę, którą gracz widzi na półce. Nazwy wariantów zostają `GrossRetail`/`NetB2B`, bo tak stoją w `Offer.price_basis`; `sim/economy` reeksportuje, więc nazwy z M5 nie drgnęły. **(2) `PolicyId(u16)`** i **(3) `ActionKind`** są **ładunkami centralnego enuma** (`DecisionReason::PolicyApplied`) i ładunek nie może pochodzić z crate'u, który od `core` zależy — ta sama reguła, która wypchnęła tu `PriceDriver` (`K-30`) i `WageCause` (`K-45`). Kolejność wariantów `ActionKind` jest kontraktem, bo `as_index()` indeksuje histogram akcji w panelu polityk, i jest ta sama co w `policy::Action`. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{PolicyApplied = 503, ManagerAssigned = 504}` domykają blok M7 otwarty przez `K-45`. Wszystkie te wartości są od tej chwili wieczne. **Czego M7c do `core` nie wnosi:** `ManagerStyle` i `Autonomy` zostają w `sim/firms` — mają jednego właściciela i jednego czytelnika, a `K-8` dotyczy słowników **wielu faz**, nie każdego enuma po kolei | M0 (właściciel `core`), M5 (oddaje `PriceBasis`), M6, M7 (wnosi), M8, M9 (edytor reguł), M12 (modding) |
| K-48 | Co M7 wnosi do `engine/core` przy finansach i upadłości | **Wykonanie `K-8` dla M7d, wpisane po M7d.** Dwa słowniki, oba z tego samego powodu co `WageCause` (`K-45`): są **ładunkami centralnego enuma** `DecisionReason`, a ładunek nie może pochodzić z crate'u, który od `core` zależy. **(1) `BankruptcyTrigger`** (`Illiquid`, `NegativeEquity`, `CourtOrder` — ładunek `BankruptcyOpened`; drugi czytelnik znany z nazwy i numeru fazy to M8, bo kara administracyjna jest `CourtOrder`, a M9 rysuje kartę firmy). Plan fazy zapisywał wariant jako `IlliquidDays { n }`; liczba dób idzie **osobnym polem powodu**, bo `vocab_enum!` daje słownik, a nie enum z ładunkiem — i tak jest lepiej, bo histogram przyczyn upadłości ma liczyć przyczyny, a nie pary (przyczyna, długość). **(2) `ClaimPriority`** (`Wages`, `Severance`, `Secured`, `Public`, `Unsecured`, `Owners` — ładunek `ClaimSettled`; M8 zgłasza roszczenie miasta jako `Public` i musi je nazwać, nie mając własnej ścieżki egzekucji, `K-10`). Tu kolejność wariantów jest **więcej niż kontraktem indeksu: jest regułą podziału**. Podział masy idzie po `as_index()` rosnąco i niższy priorytet nie dostaje ani grosza, dopóki wyższy nie jest zaspokojony w całości — dzięki temu decyzja właściciela produktu z `D7` fazy M7 (pracownicy przed wierzycielem zabezpieczonym) jest **kolejnością tej listy**, a nie gałęzią w logice. `LoanKind` dostaje przy okazji trzeci wariant `Investment`, **dopisany na końcu**, bo indeks wybiera widełki w `data/economy/bank.ron`. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{LoanTaken = 505, LeaseSigned = 506, ReceivablesFactored = 507, BondIssued = 508, BankruptcyOpened = 509, ClaimSettled = 510}` domykają blok M7 otwarty przez `K-45`; M7d **nie zajmuje żadnego numeru `StreamId`**, bo postępowanie jest deterministyczne z kolejności identyfikatorów, a nie z losowania. Wszystkie te wartości są od tej chwili wieczne. **Czego M7d do `core` nie wnosi i dlaczego:** `Creditor` z planu §5.13 **nie powstaje** — stroną zobowiązania jest `AccountOwner` z `sim/economy`, który już pokrywa pracownika, bank, dostawcę, miasto i resztę świata i ma porządek liniowy potrzebny do determinizmu podziału; drugi enum o tym samym znaczeniu rozjechałby się przy pierwszym nowym uczestniku. `ClaimOrigin` zostaje w `sim/economy`, bo niesie `LoanId`, `LeaseId` i `BondId`, czyli uchwyty do rejestrów, których `core` nie zna | M0 (właściciel `core`), M5 (oddaje `LoanKind`), M7 (wnosi), M8 (wierzyciel podatkowy), M9 (karta firmy), M10 (czynsz) |
| K-49 | Co M7 wnosi do `engine/core` przy AI firm | **Wykonanie `K-8` dla M7e, wpisane po M7e.** `core` dostaje dwa słowniki, oba z tego samego powodu co `WageCause` (`K-45`): są **ładunkami centralnego enuma** `DecisionReason`, a ładunek nie może pochodzić z crate'u, który od `core` zależy. **(1) `FirmStrategy`** (`Cautious`, `Discount`, `NicheQuality`, `AggressiveExpansion`, `Innovative`, `Consolidator` — ładunek `DecisionReason::StrategySet`; drugi czytelnik znany z nazwy i numeru fazy to M10, bo marka i R&D czytają kurs firmy, zanim zaproponują wydatek). Kolejność wariantów jest kontraktem podwójnie: `as_index()` indeksuje histogram kursów w panelu, a odwzorowanie kursu na preset polityki z `data/policies/` idzie po nazwie wariantu. **(2) `ReactionKind`** (`PriceWar`, `SupplierLock`, `Poach` — ładunek `DecisionReason::CompetitiveResponse`; M8 czyta go przy UOKiK, bo to on rozstrzyga, która z tych trzech przestaje być legalna). Trzy warianty, bo trzy są legalne: kartel i zmowa cenowa są przestępstwem i należą do M8, nie do repertuaru firmy AI. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{MarginTargetSet = 511, RestockTargetSet = 512, SiteClosed = 513, StrategySet = 514, CompetitiveResponse = 515}` domykają blok M7 otwarty przez `K-45`, a `StreamId::FirmPersonality = 222` otwiera drugą połowę bloku M7 strumieni. Wszystkie te wartości są od tej chwili wieczne. **Czego M7e do `core` nie wnosi:** `FirmPersonality` zostaje w `sim/firms` — ma jednego właściciela i jednego czytelnika, a `K-8` dotyczy słowników **wielu faz**, nie każdej struktury po kolei | M0 (właściciel `core`), M7 (wnosi), M8 (UOKiK), M9 (karta firmy), M10 (marka, R&D) |
| K-50 | Wspólne jądro ekonomii jest **regułą, a nie jednym plikiem** | **Korekta `D20` i §5.15 fazy M7, wpisana po M7e.** Plan wymieniał cztery funkcje jądra z jednym adresem — `sim/economy::kernel` — i dla dwóch z nich ten adres **jest niewykonalny**: `wage_bid` liczy `sim/firms`, `throughput` liczy `sim/supply`, a oba te crate'y stoją w grafie **przed** `sim/economy` (`economy → firms`, `economy → supply`). Przeniesienie ich zamknęłoby cykl, którego Cargo nie zbuduje; skopiowanie dałoby dwie implementacje jednej reguły, czyli dokładnie to, przed czym `K-5` ma bronić. **Rozstrzygnięcie: jądro to zbiór funkcji czystych o jawnych wejściach, z jednym adresem, a nie jeden moduł.** Implementacja mieszka w crate'cie, który ma dane; **adres nadaje `sim/economy::kernel` reeksportem**. Dla `sim/macro` (M10) różnicy nie ma — woła `kernel::wage_bid` i `kernel::throughput` i dostaje dokładnie ten kod, który liczy mezo, bo implementacja jest jedna. Wiążące pozostaje to, co było wiążące: bez ECS, bez `&World`, bez iteracji po `HashMap`, pieniądz `i64`, wejścia jawne w sygnaturze. Stan po M7e: `next_price`, `next_price_full`, `clamp_to_margin`, `take_cogs`, `ledger_post`, `annuity_payment`, `monthly_interest` **w** `sim/economy::kernel`; `wage_bid` = `magnat_firms::next_bid`, `throughput` = `magnat_supply::kernel::throughput` — reeksportowane. **Uzupełnienie po M7f:** `purchase_score` i `softmax_shares` **weszły do jądra**, bo pojawił się ich wołający — faza 4 kroku makro dzieli popyt komórki tym samym wzorem, którym mieszkaniec wybiera sklep. `purchase_score` bierze `ScoreInput` (same liczby, bez `OfferId`), a `choice::utility_of_offer` jest od tej chwili cienkim opakowaniem na nią; `softmax_shares` to reeksport `core::det_math::softmax`. **To jest cała różnica mezo vs. makro** (M10a §5.1): mezo losuje z tego rozkładu jeden sklep dla jednego agenta, makro stosuje go jako wagi | M5 (właściciel crate'a), M6, M7 (wykonuje), M10 (konsument) |
| K-51 | Kolejność wobec systemu, którego w tym scenariuszu może nie być | **`engine/ecs` dostaje `SystemDesc::after_if_present`: ograniczenie warunkowe, dla którego brak celu jest brakiem krawędzi, a nie błędem budowy.** Wpisane po M7e; poprawka w `engine/ecs` (crate M0), więc nie rozstrzyga się jej lokalnie w fazie. `after`/`before` z `K-42` wymagają, żeby cel stał w tym samym harmonogramie — i słusznie, bo literówka w nazwie ma być błędem, a nie po cichu zignorowaną krawędzią. Ale **scenariusz stawia wycinek symulacji**: `m5shop` buduje rynek bez rejestru firm, `m7labor` rejestr bez rynku, klient graficzny jedno i drugie. System, którego kolejność ma znaczenie wyłącznie wobec systemu **opcjonalnego**, nie miał czym tego wyrazić: `after` wywracał połowę scenariuszy (`m5shop` po M7e: „system economy.Market odwołuje się do nieistniejącego systemu"), a jego brak oddawał kolejność hashowi nazwy — czyli dokładnie przypadkowi, przed którym `K-42` miał bronić. Do M7e działało to **przez zbieg okoliczności**: `economy.Labor.after(firms.Firm)` nie pękało wyłącznie dlatego, że jedyny scenariusz z rynkiem pracy dodawał oba systemy. Konsekwencje wiążące: **(a)** `after`/`before` zostają bez zmian i nadal są błędem przy nieznanym celu — to jest domyślna droga i nią wyraża się kolejność w obrębie jednej fazy; **(b)** `after_if_present` używa się **tylko** tam, gdzie cel jest systemem innego crate'u, którego obecność zależy od scenariusza; **(c)** obecny cel daje dokładnie tę samą krawędź, z tym samym pierwszeństwem nad krawędzią z konfliktu dostępów (`K-42`); **(d)** `Schedule::fingerprint` nie zmienia definicji — liczy się z **krawędzi wynikowych**, więc ograniczenie, które nic nie zbudowało, nie zostawia w nim śladu. Użycia po M7e: `economy.Market → firms.Firm`, `economy.Labor → firms.Firm`, `economy.Insolvency → economy.Labor` | M0 (właściciel `ecs`), M5, M6, M7, M8, M10 — każda faza deklarująca kolejność wobec systemu spoza swojego crate'u |
| K-52 | Gdzie mieszka `Trend` | **Wykonanie `K-8` dla kierunku zmiany: nowy słownik w `engine/core::vocab`.** Wpisane po M7f. Trzech czytelników znanych z nazwy i numeru fazy: `sim/macro` (M10) go **produkuje**, `sim/firms` (M7) czyta go w tierze strategicznym, M9 rysuje z niego strzałkę w karcie firmy. Powód jest mocniejszy niż zwykła oszczędność na duplikacie: `Trend` jest **całym słownikiem**, którym prognozie makro wolno odezwać się do gracza. Model deklaruje własny błąd 3–12 % i kwota z niego jest fałszywą precyzją (`R15`), więc do UI idzie ranking, przedział albo kierunek — a kierunek to te trzy warianty i nic więcej. Kolejność wariantów (`Up`, `Flat`, `Down`) jest kontraktem, bo `as_index()` indeksuje ikonę strzałki. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{SiteOpened = 516, VoluntaryClosure = 517, FirmFounded = 518, ChainEntered = 519}` domykają blok M7 (500–599, `K-12`) i są od tej chwili wieczne | M0 (właściciel `core`), M7 (wnosi), M9 (karta firmy), M10 (`sim/macro`) |
| K-53 | System, który otwiera tick | **`engine/ecs` dostaje `SystemDesc::opens_tick`: system nim oznaczony staje w kolejności kanonicznej przed wszystkimi, które go nie mają.** Wpisane po M7f; poprawka w `engine/ecs` (crate M0), więc nie rozstrzyga się jej lokalnie w fazie. `before`/`after` z `K-42` rozstrzygają **parę**, a tu chodzi o pozycję wobec wszystkich: kolejność kanoniczna to sortowanie po `SystemId`, czyli po hashu nazwy, i z niej biorą kierunek krawędzie konfliktu dostępów. Dla systemu **wyłącznego** (`K-21`) konflikt zachodzi z każdym, więc taki system dostaje krawędź od każdego, kto w hashu wypadł przed nim; jeden jawny `before` odwraca jedną z nich i domyka cykl przez pozostałe. Tak pękł pierwszy scenariusz stawiający `firms.Firm` obok pełnego zestawu systemów mieszkańca (`economy.Market → economy.Insolvency → agents.DayLoop → traffic.Mezo → agents.Society → firms.Firm`) — a cykl miał pięć systemów i wyglądał na błąd deklaracji, którym nie był. Konsekwencje wiążące: **(a)** `opens_tick` jest **tylko** kluczem sortowania — w obrębie obu grup rozstrzyga `SystemId`, więc determinizm kolejności nie zmienia się w niczym; **(b)** `Schedule::fingerprint` nie zmienia definicji, bo liczy się z krawędzi wynikowych; **(c)** dwa systemy otwierające tick są dopuszczalne i uszeregują się między sobą kanonicznie; **(d)** deklaracja nie wymienia **ani jednego cudzego systemu**, i to jest jej istota — `sim/firms` nie wie o istnieniu ruchu i nie ma powodu, żeby wiedział. Jedyne użycie po M7f: `firms.Firm` | M0 (właściciel `ecs`), M7 (wnosi), M8, M10 — każda faza z systemem wyłącznym rozdającym pracę pozostałym |
| K-54 | Miejsce `sim/macro` w grafie zależności | **`sim/macro` stoi NAD `sim/economy`, a nie obok niego — i to przestawia adres tieru strategicznego.** Uzupełnienie `K-50`, wpisane po M7f. Reguła „zero logiki ekonomicznej w `sim/macro`" (M10a §5.1) znaczy, że model makro woła jądro, a jądro ma adres `sim/economy::kernel` — więc `macro → economy`. Zależność w drugą stronę zamyka cykl, którego Cargo nie zbuduje. Wynikają z tego trzy rzeczy wiążące. **(1) `rank_variants` i `RankedVariants` nie mieszkają w `sim/firms`**, jak zapowiadał M7 §6, tylko w `sim/macro` — `sim/firms` nie widzi `MacroOutcome` i widzieć nie może. **(2) Tier strategiczny nie mieszka w `sim/economy::ai_run`** razem z dwoma pozostałymi: jego system (`macro.WhatIf`) stoi w `sim/macro`, bo tylko stamtąd widać naraz model i rejestr firm. **(3) Wynik przechodzi przez `sim/firms`**: `StrategicOutlooks` (mapa `FirmKey → Outlook`) jest zasobem świata **zdefiniowanym w `sim/firms`**, bo to wspólny przodek obu stron — `sim/macro` pisze, `sim/economy::ai_run` czyta przy składaniu `FirmView`. To ta sama reguła, która trzyma słowniki w `engine/core` (`K-8`), zastosowana do struktury zamiast do enuma. **Zysk, którego plan nie obiecywał:** zakaz progu absolutnego na prognozie (`R14`, §7.7 pkt 4) przestaje być lintem i staje się kształtem typu — `Outlook` nie ma pola pieniężnego, a `sim/firms` nie ma czym go dodać. Ta sama droga, którą `BD-1` zamknął asymetrię informacji | M5 (właściciel `kernel`), M7 (wykonuje), M10 (właściciel `sim/macro`), M12 (tryb 50×) |
| K-55 | Słowniki, które M8 wnosi do `engine/core` | **Wykonanie `K-8` dla M8a.** `core` dostaje trzy: **`TaxKind`** (`Cit`, `Pit`, `Vat`, `Property`, `Excise`, `Duty`, `License`), **`SpendCategory`** (dwanaście kierunków wydatku publicznego) i **`AbateReason`** (`Bankruptcy`, `Council`, `TimeBarred`, `PayerGone`). Kryterium to samo co przy `PriceDriver` (`K-30`) i `WageCause` (`K-45`): są **ładunkami centralnego enuma** `DecisionReason`, a ładunek nie może pochodzić z crate'u, który od `core` zależy. Drugi czytelnik jest w każdym przypadku znany z nazwy i numeru fazy: M5 (`ChargeKind` w `TxKind::TaxPayment` niesie indeks `TaxKind`, `ProgramId` w `TxKind::PublicSpend` — indeks `SpendCategory`), M7 (miasto jako wierzyciel podatkowy w postępowaniu upadłościowym, `K-10`) i M9 (panel „Miasto"). **Słowniki są płaskie i to jest korekta planu fazy:** M8a §5.1 zapisywał `Excise(ExciseClass)`, `Duty(TariffClass)` i `License(LicenseClass)`, ale `vocab_enum!` daje słownik, a nie enum z ładunkiem — klasa idzie osobnym polem należności. Tak jest przy okazji lepiej, bo histogram wpływów ma liczyć daniny, a nie pary (danina, klasa); ta sama korekta co przy `BankruptcyTrigger` w `K-48`. Kolejność wariantów `TaxKind` i `SpendCategory` jest kontraktem **podwójnie**: indeksuje tablice `CityBudget.revenue_ytd` i `spend_ytd`, a ten sam indeks jedzie do dziennika transakcji M5, czyli do zapisu gry. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{TaxAssessed = 600, TaxSettled = 601, TaxOverdue = 602, TaxAbated = 603, PublicSpend = 604, MunicipalBondIssued = 605, BudgetDeficitClosed = 606}` otwierają blok M8 (600–699, `K-12`). **M8a nie zajmuje ani jednego numeru `StreamId`**: naliczenie daniny jest funkcją stanu, a nie losowaniem, więc blok 240–259 zostaje w całości wolny dla M8c (zdarzenia) i M8d (kontrole). Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M5, M7, M8 (wnosi), M9 |
| K-56 | Katalog `data/city/` | **Dopisany do §5.** Stawki podatkowe i polityka wydatkowa miasta są **polityką, nie projektem**: mają się zmieniać uchwałą rady (M8e) i przebiegiem balansatora, a wynik ma być widoczny w diffie, a nie w rekompilacji. To ten sam argument, który wypchnął diagram podstawowy ruchu do `data/roads/vdf.ron` (`K-24`) i progi kaskady do `data/tuning/supply.ron` (`K-35`). **Dlaczego nowy katalog, a nie któryś z istniejących:** `data/economy/` jest własnością M5 i nie ma być workiem na cudze liczby (`K-35`), a `data/tuning/` niesie liczby, **które wolno przestawić bez zmiany znaczenia modelu** — stawka VAT-u zmienia cenę, którą widzi gracz, więc zmienia kształt, a nie kalibrację. Dwa pliki: `tax.ron` (siedem danin, klasy VAT przypisane **przez domenę klucza towaru** tak samo jak klasy taryfowe w `AD-12`, stawki akcyzy per klucz towaru, opłaty koncesyjne per klucz rodzaju zakładu, terminy płatności, odsetki i przedawnienie) oraz `budget.ron` (udziały planu wydatków per `SpendCategory`, rezerwa, próg cięcia, parametry emisji obligacji i sufit zadłużenia). Lista katalogów w §5 deklaruje się jako kompletna, więc brak wpisu byłby jej błędem, nie luką | M8 (właściciel), M9 (edytor polityki), M12 (modding) |
| K-57 | Hak `TaxEngine` rozszerzony, nie podmieniony | **Wykonanie kontraktu §6 fazy M8 wobec M5.** `sim/economy::TaxEngine` miał dwie metody (`gross_from_net`, `net_from_gross`) i dostaje trzy kolejne, **wszystkie z ciałem domyślnym**, więc `NoTax` nie zmienia się w niczym: `vat_on_gross` (część podatkowa kwoty brutto — to, co wpisuje się w `TxMemo.tax`), `excise_on` (akcyza od masy wyrobu obłożonego) i `withhold` (potrącenie u źródła przy wypłacie, po indeksie encji gospodarstwa). **Korekta planu fazy:** M8a §5.1 rysował trait z metodami `tax_on(&mut self, …)` i `record(…)`, czyli silnik **zapisujący** do rejestru. Taki trait nie istnieje i istnieć nie może — hak siedzi w `Market` jako `Box<dyn TaxEngine>` za `Arc<Mutex<…>>`, więc dostaje `&self`; rejestr należności mieszka w zasobie `City` i napełnia go system miasta, a nie rynek. **Gdzie stoi każdy z trzech haków i dlaczego akurat tam:** `vat_on_gross` przy rozliczeniu transakcji detalicznej, bo `TxMemo.tax` jest polem tego przelewu; `excise_on` przy sprzedaży detalicznej **i** przy rozliczeniu hurtowym, bo wyrób obłożony zmienia właściciela w obu miejscach; `withhold` w `pay_incomes`, bo gospodarstwo ma dostać **netto** — potrącenie doliczone później znaczyłoby, że planer kopert dzieli dochód, którego nikt nie dostał, i pierwszy miesiąc z podatkiem zamieniłby połowę miasta we wnioskodawców kredytowych. Konsekwencja wiążąca: faza dokładająca daninę naliczaną na zdarzeniu gospodarki dokłada **metodę z ciałem domyślnym**, a nie drugi trait — dwa haki w jednym `Market` rozjechałyby się przy pierwszej podmianie | M5 (właściciel `TaxEngine`), M8 (wnosi), M9, M10 |
| K-62 | `Subject` — jeden uchwyt do każdej encji, w `engine/core` | **Wpisane po decyzji właściciela produktu z 2026-09-17: każdy obiekt widoczny w świecie ma być klikalny, a każda nazwa w karcie inspekcji — odnośnikiem do podmiotu, o którym mówi.** Do dziś takiego uchwytu nie było: `00` §2 daje dwanaście osobnych newtype'ów (`CitizenId`, `FirmId`, `SiteId`, `VehicleId`, …), `K-16` wyjmuje `BatchId`/`OfferId` poza ECS, a `magnat_ui::Selection` obejmuje **trzy** z nich i żyje w `engine/ui`. `Subject` (definicja w `M9c` §5.7) idzie do `engine/core` z tego samego powodu co `PriceDriver` (`K-30`) i `WageCause` (`K-45`): **jest ładunkiem `DecisionReason`**, a ładunek nie może pochodzić z crate'u, który od `core` zależy. Bez tego „wybrała »Dobry Koszyk«" jest napisem, a nie odnośnikiem do tego zakładu, i żadna faza `sim/*` nie może zbudować powodu wskazującego na konkretny podmiot. Trzy konsekwencje wiążące: **(1)** `magnat_ui::Selection` przestaje być osobnym typem i staje się `Option<Subject>` — dwa równoległe słowniki „co da się zaznaczyć" rozjechałyby się przy pierwszej nowej encji; **(2)** faza dokładająca byt, o który gracz może zapytać „dlaczego", dokłada wariant `Subject` **i** układ zakładek jego karty — tak samo, jak dziś dokłada wariant `DecisionReason` i ramienia renderu (`K-12`); **(3)** `Subject::resolve` zwraca `Option`, a `None` jest normalnym stanem świata (firma upadła, mieszkaniec zmarł), nie błędem. Podział `DecisionReason` na trzy enumy po aktorze (`K-58`, R2e) tego nie dotyka: `Subject` jest ładunkiem każdego z trzech, a nie częścią któregokolwiek. **Nie jest to uniwersalny `EntityRef`** — `Subject` adresuje to, co ma **kartę**, a nie każdą encję ECS; usługa publiczna jedzie jako `Site` (`PublicService.site` to `SiteId`), a wybory jako zakładka karty rady | M0 (właściciel `core`), M9 (autor karty), M11 (bufor ID), M3/M5/M6/M7/M8/M10 (dokładają warianty) |
| K-60 | Wiek produkcyjny jest **daną**, nie stałą w kodzie | **Jedno źródło: `data/demography/demography.ron`, pole `ages.labour_force`.** Wpisane przy starcie M8c jako wykonanie decyzji `D-N1` (pozycja 7 wykazu `R2`, wyprzedzona przed R2, bo blokowała kalibrację hazardów). Do tej chwili w repozytorium były **trzy** definicje wieku produkcyjnego: `work_start: 18` i `retirement: 65` w danych, oraz `const WORKING_AGE: RangeInclusive<_> = 16..=74` **dwa razy w kodzie, w dwóch różnych typach całkowitych** (`sim/economy/src/labor/system.rs`, `sim/macro/src/lift.rs`) — i nikt nie wiedział, że jest powielona. Rozróżnienie dwóch pojęć zostaje i jest słuszne: kadry liczą etaty od `work_start`, statystyka publiczna liczy siłę roboczą szerzej, bo szesnastolatek bez pracy i bez szkoły jest bezrobotny. Problemem nie było istnienie dwóch pojęć, tylko to, że **jedno z nich nie miało źródła**. Konsekwencja wiążąca: stopa bezrobocia (`sim/economy::labor`) i `MacroCell::in_labour_force` (`sim/macro`) czytają **to samo pole**, walidator sprawdza `labour_force.min >= ages.school_start` i `labour_force.max <= ages.max`, a `DEMOGRAPHY_SCHEMA_VERSION` idzie z 1 na 2. Sonda `UnemploymentPermille` generatora zdarzeń M8c stoi na tej liczbie, więc bez tego wpisu kalibracja krzywych protestu i fali przestępczości mierzyłaby własny rozjazd | M3 (właściciel tabeli), M5, M7, M8, M10 |
| K-63 | Słowniki, które M8 wnosi do `engine/core` przy zdarzeniach | **Wykonanie `K-8` dla M8c.** `core` dostaje trzy pozycje. **(1) `EventCategory`** (`Natural`, `Infrastructure`, `External`, `Social`, `Firm`, `Political` — sześć kategorii z PRD §11.2) jest **ładunkiem** `DecisionReason::EventStarted`, a ładunek centralnego enuma nie może pochodzić z crate'u, który od `core` zależy — ta sama reguła, która wypchnęła tu `PriceDriver` (`K-30`) i `WageCause` (`K-45`). Kolejność wariantów jest kontraktem, bo `as_index()` indeksuje limit `max_concurrent` per kategoria i histogram zdarzeń na rok w raporcie balansatora (ryzyko `R1` fazy). **(2) `EventId(u32)`** z tego samego powodu, plus drugi konsument znany z nazwy i numeru fazy: `Subject::Event(EventId)` (`K-62`) robi z wpisu w kronice odnośnik do karty zdarzenia, a nie napis. Numer jest **monotoniczny w obrębie gry i nigdy nie wraca** — zdarzenie, które się skończyło, zostaje w kronice pod swoim numerem na zawsze. **(3) `Season`** (`Winter`, `Spring`, `Summer`, `Autumn`) — rok gry ma 360 dób (`K-1`), więc sezon to równe 90 dób i nie ma sporu o granicę. Trzech czytelników: bramka sezonowa katalogu zdarzeń (M8c), kalendarz agrotechniczny (M6) i UI. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{EventStarted = 609, EventEnded = 610}` przedłużają blok M8 (600–699, `K-12`), a `StreamId::{EventRoll = 241, EventSeverity = 242, EventDuration = 243, Weather = 244}` zajmują numery zarezerwowane imiennie w M8a §5.0. **`StreamId::EventHazard = 240` zostaje niezajęty i takim zostanie**: hazard jest funkcją stanu świata, a nie losowaniem, więc strumień „na losowanie prawdopodobieństwa" opisywałby mechanizm, którego ta faza świadomie nie ma. Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M8 (wnosi), M6, M9 (kronika i karta zdarzenia), M10 |
| K-64 | Słowniki i **jedna struktura**, które M8 wnosi do `engine/core` przy usługach publicznych | **Wykonanie `K-8` dla M8d, z wykonaniem `CF-2`.** `core` dostaje cztery słowniki i jedną strukturę danych. Słowniki: **`ServiceKind`** (`School`, `Clinic`, `Hospital`, `Police`, `Fire`, `Waste`, `Park`, `Office` — ładunek `DecisionReason::ServiceQuality`), **`AgencyKind`** (pięć urzędów kontrolnych — ładunek `CaseOpened` i `RemedyImposed`), **`RemedyKind`** (`Fine`, `Closure`, `LicenseRevoked`, `ForcedDivestiture`, `BackTax` — ładunek `RemedyImposed`; wariant z kwotą mieszka w `sim/city::Remedy`, bo kwota i termin to nie jest słownik — ta sama korekta co przy `BankruptcyTrigger` w `K-48`) i **`PermitKind`** (osiem rodzajów pozwolenia — ładunek `PermitIssued`). Kryterium to samo co przy `PriceDriver` (`K-30`): ładunek centralnego enuma nie może pochodzić z crate'u, który od `core` zależy. **Piąta pozycja nie jest enumem i to jest jej istota: `ServiceCoverage`** — tablica `dzielnica × ServiceKind` w skali `Q`, zasób świata, który `sim/city` **zapisuje**, a `sim/agents` (tempo nauki dziecka, długość zwolnienia) i `sim/economy` (ubytki inwentaryzacyjne sklepu) **czytają**. Adres jest wymuszony grafem: obaj czytelnicy stoją **pod** `sim/city`, więc gdyby tablica mieszkała u piszącego, żaden z nich nie mógłby jej zobaczyć. To ten sam ruch, którym `K-34` przeniósł tu `OpenHours` — struktura bez logiki, wspólna dla faz — i to samo odwrócenie zależności, którym `sim/events` nakłada parametry (`CE-4`): piszący sięga do pola czytelnika. `core` nie dostaje przy tym ani jednej reguły: jak liczy się jakość placówki i jak zanika ona z odległością, wie wyłącznie `sim/city`. Kolejność wariantów `ServiceKind` jest kontraktem podwójnie — indeksuje wiersz `ServiceCoverage` i tablice kalibracji w `data/tuning/city.ron`. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{ServiceQuality = 611, PermitIssued = 612, CaseOpened = 613, RemedyImposed = 614, ShadowShareSet = 615}` przedłużają blok M8 (600–699, `K-12`), a **M8d nie zajmuje ani jednego numeru `StreamId`** — kontrola skarbowa jest zdarzeniem i losuje `StreamId::EventRoll` (`CF-1`), a wszystkie cztery pozostałe urzędy działają z progu na stanie świata. Numery 245, 247, 249–259 bloku M8 zostają wolne. Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M8 (wnosi), M3 (czyta pokrycie), M5 (czyta pokrycie), M7, M9 (karta usługi i karta sprawy), M10 |
| K-65 | Katalog `data/tuning/city.ron` | **Dopisany do §5 jako trzeci plik `data/tuning/`** (`K-35`), obok `supply.ron` (M6) i `grid.ron` (M8b). Niesie kalibrację usług publicznych, urzędów i egzekucji: wagi jakości placówki i normy finansowania na osobę, zanik pokrycia poza dzielnicą, przerób urzędu i koszt wniosku w jednostkach, parametry szarej strefy, progi i stawki pięciu urzędów kontrolnych oraz bazową stawkę ubytków inwentaryzacyjnych. Rozróżnienie wobec `data/city/` jest to samo, które `K-56` postawiło między `data/city/` a `data/tuning/`: **tam siedzi stawka VAT-u, która zmienia cenę widzianą przez gracza, czyli kształt; tutaj próg, przy którym urząd otwiera sprawę, czyli kalibracja.** Plik stroi balansator (M8e) i to on odpowiada za jego wartości — `shadow.distress_bp` decyduje o tym, czy ryzyko `R10` („albo wszyscy oszukują, albo nikt") jest zamknięte, a `agencies.evidence_per_inspector_day` o tym, czy sprawy w ogóle się kończą. Właściciel: M8 | M8 (właściciel), M9 (panel miasta), M12 (modding) |
| K-66 | Wariant `TxKind` dla wpłaty na kampanię | **`TxKind::CampaignDonation { candidate: u8, illegal: bool }` dopisany na końcu enuma M5.** Wpisane po M8e. `TxKind` należy do `sim/economy` i `K-57` ustalił już, że faza dokładająca daninę dokłada **metodę z ciałem domyślnym**, a nie drugi trait — tu chodzi o tę samą regułę zastosowaną do dziennika: pieniądz rusza się wyłącznie przez `Books::transfer`, a `transfer` wymaga rodzaju zapisu. Kampania wyborcza jest przepływem, którego żaden istniejący wariant nie opisuje: to nie jest wynagrodzenie, nie zakup, nie danina i nie wydatek publiczny. Bez wariantu wpłata musiałaby albo udawać któryś z nich (i zafałszować bramkę G9 balansatora, która dzieli zapisy na wybory i zobowiązania), albo nie ruszać pieniądza wcale — a wtedy poparcie kandydata byłoby deklaracją w tabeli, nie kosztem. **Odbiorcą jest `RestOfWorld`** i to jest jawny sufit: kampania kupuje czas w mediach, a media są firmami dopiero w M10 (`D7`); nadawca jest za to prawdziwy i to on jest treścią wariantu. Konsekwencja wiążąca: `jest_decyzja` w `tools/balansator` klasyfikuje wpłatę jako **wybór**, bo nikt do niej nie zobowiązał firmy. Wartości pól są wieczne tak samo jak reszta wariantów `TxKind`, bo dziennik wchodzi do zapisu gry | M5 (właściciel `TxKind`), M8 (wnosi), M9 (gracz wpłaca), M10 (media jako odbiorca) |
| K-67 | Słowniki, które M8 wnosi do `engine/core` przy władzy i wyborach | **Wykonanie `K-8` dla M8e.** `core` dostaje trzy: **`PolicyKind`** (dziewięć rodzajów uchwały rady — ładunek `DecisionReason::{PolicyEnacted, TaxRateChanged}`), **`TenderKind`** (cztery przedmioty przetargu — ładunek `TenderPublished`/`TenderAwarded`) i **`VoteDriver`** (sześć motywów głosu — ładunek `VoteCast`). Kryterium to samo co przy `PriceDriver` (`K-30`) i `ServiceKind` (`K-64`): ładunek centralnego enuma nie może pochodzić z crate'u, który od `core` zależy. Wszystkie trzy są **płaskie**, a kwoty, progi i identyfikatory przedmiotu idą osobnymi polami — ta sama korekta co przy `BankruptcyTrigger` (`K-48`) i `RemedyKind` (`K-64`). Sam `Policy` — z kwotami, oknem tygodniowym i zbiorem krawędzi — zostaje w `sim/city`, bo to nie jest słownik. **`PolicyKind` ma dziewięć wariantów, a nie jedenaście, i to jest rozstrzygnięcie, nie przeoczenie:** `Zoning` nie powstaje, bo strefa jest wejściem generatora miasta i po Etapie 7 nikt jej nie czyta w trakcie gry, a `ParkingFee` — bo cennik parkingu siedzi za `&mut` w routerze podróży, do którego uchwała nie sięga. Wariant, którego skutku nikt nie widzi, przechodzi każdy test i wygląda tak samo jak działający (`R2`). Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{PolicyEnacted = 616, TaxRateChanged = 617, TenderPublished = 618, TenderAwarded = 619, ElectionHeld = 620, VoteCast = 621, CampaignBacked = 622}` domykają blok M8 (600–699, `K-12`), a `StreamId::{Election = 245, TenderScoring = 250}` zajmują dwa numery zarezerwowane imiennie w M8a §5.0. **`StreamId::CityPolicy = 249` zostaje niezajęty i takim zostanie** — decyzja burmistrza jest funkcją stanu miasta i wag jego preferencji, a remis rozstrzyga kolejność `PolicyKind`; strumień „na wybór działania" opisywałby losowość, której ta faza świadomie nie ma, bo burmistrz rzucający kostką nie dałby się przewidzieć graczowi. Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M8 (wnosi), M9 (edytor polityki, karta rady), M10 (media, związki) |
| K-68 | Kto jest właścicielem drogi, którą powstaje świat | **`game/` stawia świat, `tools/headless` jest jego konsumentem — nie odwrotnie.** Wpisane przy starcie M9a; koryguje `K-32`. Mosty „parametry → teren → miasto → ludzie → gospodarka → firmy → strona publiczna → sieci → zdarzenia" (`population`, `retail`, `plants`, `firms`, `labor`, `city`, `grid`, `events`, `full`) przeprowadziły się z `tools/headless` do **`magnat-game::world`**; `magnat-headless` reeksportuje je pod starymi nazwami, więc ani jeden scenariusz, test integracyjny ani balansator nie zmienił wywołania. Powód jest twardy, a nie porządkowy: kryteria WP1 i WP13 fazy M9 żądają, żeby przebieg bezgłowy **uruchamiał sesję gry**, czyli `headless → game`; gdyby `game` zależał od `headless` po mosty, Cargo miałby cykl i nie zbudowałby workspace'u. Druga kopia mostów odpada z tego samego powodu co zawsze (`K-8`): rozjeżdża się z pierwszą, a rozjazd widać dopiero jako inny wynik bramki. **Konsekwencja wiążąca, i to ona jest tu istotą:** stawianie świata ma od tej chwili **jedno** miejsce — `game::world::stand_up` — i woła je zarówno klient graficzny, jak i przebieg bezgłowy. Do M8e klient składał świat sam i składał go **inaczej** (bez firm, bez strony publicznej, bez sieci, bez zdarzeń), więc okno pokazywało inne miasto niż to, które mierzył balansator, a różnicy nie widział nikt, bo nikt nie porównywał. Faza dokładająca krok stawiania świata dokłada go **tam**, a nie w swoim scenariuszu. Co zostaje bez zmian z `K-32`: `tools/headless` dalej ma target biblioteczny i dalej wystawia to, co ma więcej niż jednego konsumenta — tyle że jest to teraz rusztowanie **scenariuszy**, a nie gry. Dług nazwany w `K-32` pkt (d) (nazwa crate'u przestała opisywać zbiór konsumentów) tym się nie domyka i zostaje w R1 | M0 (właściciel `tools/headless`), M9 (właściciel `game/`), M5–M8 (mosty), M12 (modding) |
| K-69 | Gdzie mieszkają identyfikatory pięciu ostatnich wariantów `Subject` | **Wykonanie `K-62` i domknięcie `DE-9`, wpisane po M9c.** `Subject` dostaje pięć brakujących wariantów i ma ich od tej chwili szesnaście. Rozstrzygnięcie dzieli się na dwa, bo dwa rodzaje identyfikatorów. **(1) `TenderId`, `CaseId` i `PermitId` przenoszą się z `sim/city` do `engine/core::types`** — ten sam ruch co przy `RoadClass` (`K-23`), `GateKind` (`K-33`) i `DepositId` (`K-39`), i z tego samego powodu: `core` nie może zależeć od `sim/city`, a duplikat rozjechałby się przy pierwszej zmianie. `sim/city` reeksportuje, więc nazwy z M8d i M8e nie drgnęły; zostają tam `Tender`, `Case`, `Permit` i `Remedy`, bo to są **struktury z kwotami i terminami**, a nie identyfikatory. **(2) `Batch` i `Offer` jadą jako `ArenaRef`** — uchwyt areny z **zatartym parametrem typu**, nowy w `engine/core::arena`. Powód jest twardy: `K-16` wyjął partie i oferty poza ECS i sparametryzował ich uchwyty typem zawartości (`ArenaHandle<Batch>`), a zawartości `core` nie zna i znać nie ma. Zatarcie jest tańsze niż przeniesienie obu struktur i uczciwsze niż surowe `u64` w publicznym enumie, bo nazywa moment przejścia granicy: `ArenaRef::of` po jednej stronie, `ArenaRef::to_handle` po drugiej. Cena jest jawna: pomylenie areny przestaje być błędem kompilacji i staje się pustą kartą, więc `to_handle` wolno wołać **wyłącznie** tam, gdzie wiadomo, o którą arenę chodzi. Generacja zostaje bez zmian, więc uchwyt po zwolnieniu nadal nigdy nie jest ponownie ważny. **Konsekwencja wiążąca dla faz następnych:** faza dokładająca byt z kartą dokłada wariant `Subject` **i** ramię w `game::inspect::card` — tam jest wyczerpujący `match`, więc brak ramienia łamie kompilację `game/`, tak samo jak brak ramienia renderu przy `DecisionReason` (`K-12`) | M0 (właściciel `core`), M8 (oddaje trzy identyfikatory), M9 (autor karty), M6, M5, M10–M12 |
| K-70 | Co M9 wnosi do `engine/core` i do `data/` przy języku reguł | **Wykonanie `K-8` i `K-4` dla M9d.** Trzy rzeczy, wszystkie drobne i wszystkie wieczne. **(1) `StreamId::PolicyExecution = 260`** otwiera blok M9 (260–279, `K-4`) — **jeden** strumień na dwa losowania jakości wykonania polityki (błąd wykonania i pominięty cykl), bo klucz `(indeks menedżera, tick)` je rozdziela, a numer `StreamId` jest nieodwracalny. Opóźnienie informacji i zwłoka reakcji **nie losują**: pierwsze jest wiekiem obrazu konkurencji z M5c, drugie wydłużeniem martwej strefy, a oba są funkcjami umiejętności. **(2) `DecisionReason::PolicyApplied` rośnie o dwa pola:** `lag_days: u8` (wiek danych, na których menedżer pracował) i `deviation_bp: i16` (o ile spudłował wobec wartości wyliczonej przez regułę). To jest zmiana **ładunku istniejącego wariantu**, nie nowy wariant, i dlatego wymaga wpisu tutaj, a nie tylko w dokumencie fazy. Uzasadnienie jest jedno i wystarcza: obu liczb da się dowiedzieć **wyłącznie w chwili wykonania** — dzień później obraz konkurencji jest już inny, a rzut menedżera nie zostawia śladu nigdzie indziej. Reszta wejść reguły (`inputs`, `target`) **nie wchodzi do powodu i nie wejdzie**: `Metric` mieszka w `sim/policy`, a `core` od niego nie zależy; odtwarza je na żądanie `magnat_policy::inputs` przy otwarciu karty — i to jest powód, dla którego tamta funkcja istnieje. Producentem powodu zostaje `sim/policy` (z zerami), a **uzupełnia go wykonawca** w `sim/economy`, bo tylko on zna menedżera; drugi wariant powodu („PolicyAppliedByManager”) byłby dla gracza tym samym zdarzeniem opisanym dwa razy. **(3) `data/tuning/policy.ron`** — krzywa „umiejętność zarządzania → jakość wykonania” (opóźnienie informacji, zwłoka reakcji, rozrzut wykonania, szansa pominięcia; po dwa krańce na każde) plus podłoga „bez nadzoru”. Kryterium `data/tuning/` z `K-35` jest spełnione wprost: to są liczby, **które wolno przestawić bez zmiany znaczenia modelu**, a stroi je balansator. Wykonanie decyzji otwartej nr 5 fazy M9 wg propozycji domyślnej: właścicielem umiejętności jest M7, właścicielem odwzorowania M9, a odwzorowanie jest daną. Konsekwencja dla sygnatury: `ManagerExecution::from_skill(skill, &ManagerCurve)` bierze krzywą argumentem — dokument M9d pisał `from_skill(skill)`, bo pisał ją przed decyzją o danych. **Czego M9d do `core` nie wnosi:** ani jednego słownika. Metryki, akcje i diagnostyka języka mieszkają w `sim/policy` (`K-11`), a `ActionKind` przeniósł tam `K-47` — `K-8` dotyczy słowników **wielu faz**, nie każdego enuma po kolei | M0 (właściciel `core`), M5 (wykonawca w `sim/economy`), M7 (producent powodu), M9 (wnosi), M10 |
| K-71 | Co M9 wnosi do `data/` i do `engine/core` przy panelach i scenariuszach | **Wykonanie `K-8` i uzupełnienie §5 dla M9e.** Trzy rzeczy, wszystkie drobne i wszystkie wieczne. **(1) `data/scenarios/scenarios.ron`** — katalog scenariuszy rozgrywki: klucz, tytuł i opis jako `LocKey`, łatki świata, cele i limit czasu. **Kolejność wpisów jest kontraktem zapisu gry**, bo `ScenarioId` w kopercie `StartGame` jest pozycją w tej liście; klucz tekstowy jest drugą tożsamością i nie zmienia się nigdy. Jeden plik, a nie `*.ron` po jednym na scenariusz: katalog `data/scenarios/` należy od M6 do zapasu startowego zakładów (`initial_stock.ron`), więc ładowarka po masce próbowałaby go wczytać jako scenariusz. Właściciel pliku: M9; format zapowiadał §5 słowami „format scenariusza opisanego danymi projektuje M9". **(2) `ArenaRef` dostaje `Serialize`/`Deserialize`** — uchwyt areny z zatartym typem (`K-69`) jedzie w `ViewCommand::Follow`, bo partia jest celem trybu „śledź", a partie są poza ECS (`K-16`). Derywacja, nie nowy mechanizm: generacja zostaje bez zmian, więc uchwyt po zwolnieniu nadal nigdy nie jest ponownie ważny. **(3) `CardTabKind` rośnie o `History`** — kolejność wariantów jest kolejnością rysowania zakładek, a `History` wchodzi **przed** `Day`, bo czyta się ją jako „stan → skąd się wziął → co dalej". Wariant `Actions` **nie powstaje**: potrzebuje szablonu komendy oderwanego od panelu, a każda komenda ma dziś swój panel i swój kontekst (`DI-12`). **Czego M9e do `core` nie wnosi:** ani jednego słownika i ani jednego numeru `StreamId`. Panele niczego nie losują, warunki zatrzymania są funkcją stanu, a cele scenariusza zapytaniem o świat — blok M9 (260–279) zostaje przy jednym zajętym numerze z `K-70`. Blok `DecisionReason` 700–799 zostaje w całości wolny, tak jak zostawił go `DG-1` | M0 (właściciel `core`), M6 (właściciel katalogu `data/scenarios/`), M9 (wnosi), M10, M11, M12 (modding scenariuszy) |
| K-74 | **`has_job()` nie znaczy „pracuje"** — uczeń, szkoła i wykształcenie jako stan zmienny | **Wykonanie pozycji 1 i 21 wykazu `R2` (pakiety `R2-WP1` i `R2-WP5`), wyprzedzone przed R2 zgodnie z `D-N1`.** `Employment.site` niesie **szkołę** ucznia (`E-19` w M3d), więc `has_job()` — czyli `site != NO_SITE` — było prawdą także dla siedmiolatka. Konsekwencji nikt nie zapisał i były trzy, każda cicha. **(1)** Uczeń wchodził do indeksu miejsc pracy `SocialIndex::by_site` i dostawał relacje `Colleague` z klasą, a `M10e` §5.9 liczy warunek powstania związku zawodowego na spójnej składowej tego indeksu — pierwszą kandydatką do uzwiązkowienia była szkoła podstawowa. **(2)** Rynek pracy widział go jako zatrudnionego, z rolą odziedziczoną po losowaniu cech. **(3)** Dobowy cykl życia, który od M8c stawiał flagę ucznia, pytał `has_job()` w znaczeniu „pracuje" — więc rocznik z Etapu 8 tracił flagę przy pierwszej swojej dobie shardu, zachowując przy tym przypisaną szkołę. **Rozstrzygnięcie: `Employment::is_employed()` = `has_job() && !is_pupil()`**, i to jej używa każdy kod liczący zatrudnienie, płace, relacje w zakładzie i statystykę pracujących; `has_job()` zostaje tam, gdzie pytanie brzmi „czy `site` na coś wskazuje" (`release_job_of`, `site_of`, dojazd w karcie). Do tego **cykl szkolny dostaje treść**: wejście do szkoły przydziela placówkę (`places::nearest_school` — **jedna reguła** dla Etapu 8 i dla dobowego cyklu), wyjście ją zwalnia i podnosi `Vitals::edu_level` wg tabeli `education` z `data/demography/demography.ron` — **ale tylko temu, kto placówkę miał**: miasto bez szkoły w zasięgu zostawia dziecko z samą flagą wieku szkolnego, a osiemnastka wychodzi z zerem. To jest cały wpływ sieci placówek na kapitał ludzki, dopóki M8d nie doda jakości pojedynczej szkoły. Trzy konsekwencje wiążące: **(a)** `engine/core` dostaje zasób `PlaceCatalog` w `sim/agents` — ten sam `Arc<PlaceTable>`, który ma `Market` i `TrafficOracle`, nie druga kopia; nie wchodzi do hasha, bo katalog miejsc jest daną miasta; **(b)** `LifeEventKind` dostaje wariant **`LeftSchool` na końcu** listy (kolejność jest kontraktem zapisu gry) razem z ramieniem renderu i kluczami w obu językach; **(c)** `DEMOGRAPHY_SCHEMA_VERSION` idzie z 2 na 3 — doszła tabela `education` (lata nauki → `EduLevel`). **`ages.childcare_end` tam nie wchodzi**, choć `R2a` je zapowiadało: opieka nad dzieckiem poniżej wieku szkolnego blokuje dorosłego, czyli zmienia podaż pracy całego miasta — to jest własna naprawa z własnym przebiegiem balansatora (`R2` §3 pkt 2, pozycja 66 wykazu), a liczba bez czytelnika wygląda w danych tak samo jak działająca. **Czego ten wpis nie robi:** nie zajmuje numeru `StreamId`, choć `R2a` taki numer zapowiadał — wybór placówki jest deterministyczny bez losowania (najbliższa, przy równej odległości niższy klucz), więc strumień opisywałby mechanizm, którego nie ma. Tabela `education` **nie dostaje osobnego pliku**, bo walidator musi ją sprawdzić wobec `ages` i osobny plik powtarzałby te same granice w drugim miejscu | M3 (właściciel demografii i `Employment`), M0 (`core`), M5, M7 (rynek pracy), M8 (usługi publiczne), M10 (`K-9`, związki zawodowe) |
| K-75 | Rozliczenie B2B niesie **zakład** sprzedawcy i jego koszt własny | **Wykonanie pozycji 2 wykazu `R2` (pakiet `R2-WP7`), wyprzedzone przed R2 zgodnie z `D-N1`.** `supply::Settlement` niósł sprzedawcę jako `FirmId`, a rachunek wyniku jest **per zakład** (`SitePnlMonth`) — firma z trzema zakładami dostawała jedną kwotę i nie było jej jak rozdzielić na linie. Skutek: `Market::close_month_with` iteruje po sklepach, `Firms::post_revenue` ma w symulacji jednego wołającego (tę pętlę), więc zakład produkcyjny bez półki nie dostawał wpisu **nigdy**, a `SitePnlMonth.revenue` zostawał zerem. Zero znaczy w `margin_bp()` „nie wiem", nie „strata", a tier taktyczny AI przerywa liczenie miesięcy straty na pierwszym miesiącu bez pomiaru — fabryka była **strukturalnie odporna na zamknięcie**, niezależnie od tego, jak długo przynosiła straty. **Rozstrzygnięcie:** `Settlement` dostaje `seller_site: Option<SiteId>` (`None` = import zza granicy) i `seller_cogs: Money`. Obie liczby są znane **w chwili konstrukcji** i pole tylko je przenosi: `Quote` niesie `from_site`, kontrakt `deliver_from`, a koszt własny liczy `Store::resell` — to samo miejsce, które dopisuje go do księgi kontrolnej magazynu, więc marża zakładu jest z partii, a nie z miesięcznej średniej. Utarg zbiera `Market::absorb_settlements` (strona odbierająca jako jedyna zna naraz zakład sprzedający, kwotę i koszt) do licznika miesięcznego, a `close_month_with` wypuszcza go **tą samą listą**, którą oddaje sklepom — `post_revenue` zostaje przy jednym wołającym. Konsekwencje wiążące: **(a)** licznik miesięczny wchodzi do hasha stanu, bo między dostawą a domknięciem miesiąca jest jedynym śladem po wyniku zakładu — ta sama zasada, co przy `b2b_outbox`; **(b)** `SitePnlMonth` zakładu produkcyjnego **przestaje być zerem**, więc hash stanu zmienia się w każdym świecie z produkcją; **(c)** zakład, który w danym miesiącu nic nie wysłał, **nie dostaje wpisu** — wpis z zerowym utargiem znaczyłby znowu „nie wiem". Utarg jest **netto** (`K-7`): cło idzie do miasta, nie do sprzedawcy. Eksport przez węzeł graniczny nie przechodzi przez `resell`, więc niesie koszt własny zero i to jest jawny sufit nazwany w kodzie | M5 (właściciel `Market` i `post_revenue`), M6 (właściciel `Settlement`), M7 (tier taktyczny, karta firmy), M10 (przejęcia) |
| K-76 | Co M11 wnosi do `engine/core` i do `data/` przy formacie modelu i snapshocie | **Wykonanie `K-4` i `K-8` dla M11a.** Cztery rzeczy i wszystkie od tej chwili wieczne. **(1) `StreamId::{Appearance = 300, Interior = 301}`** otwierają blok M11 (300–319, `K-4`); rezerwa 302–319. `Appearance` losuje wygląd mieszkańca kluczem `(indeks encji, tick 0)`, `Interior` rozstawi propy we wnętrzu w M11c — numer jest zarezerwowany imiennie już teraz, bo blok przydziela się fazie, a nie podfazie. **(2) Kolejność w dwóch katalogach z §5 jest kontraktem zapisu gry.** `data/palettes/palettes.ron` niesie osie `districts` i `epochs`; indeks pary jest `DistrictPaletteId` i jedzie w buforze instancji, więc dopisywać wolno wyłącznie na końcu — to ta sama reguła, co przy klasach pojazdów (`K-24`) i pulach imion (`K-28`). W `data/models/` jest odwrotnie i też celowo: `ModelId` jest **pozycją posortowanego klucza tekstowego**, więc w zapisie trzyma się klucz, a nie indeks (§5), i dołożenie modelu niczego nie przenumerowuje. Same katalogi były w §5 od decyzji 9.8 fazy M11 — ten wpis dokłada im regułę, nie adres. **(3) `RenderSnapshot` przestaje być `Copy`** i to jest zmiana kontraktu, bo §4 wymaga podwójnego buforowania, a M1 zapisał je jako strukturę kopiowalną. Kontraktem było i zostaje **zero alokacji na publikację**; spełnia je bufor o stałej pojemności alokowanej raz (`CapVec`) plus para buforów zamienianych indeksem (`SnapshotPair`). `Copy` nigdy nie było wymaganiem — było wygodą przy czterech polach, a przy 1,31 MB ładunku znaczy kopiowanie megabajta przy każdym przekazaniu i przepełnienie stosu przy pierwszej konstrukcji. **(4) Wygląd encji nie jest komponentem i nie wchodzi do hasha stanu.** `Appearance` mieszkańca, lakier pojazdu i oklejenie firmowe są **funkcjami czystymi** od ziarna, indeksu encji i stanu, który świat już ma (zawód, zamożność, właściciel). Zapisane w ECS byłyby drugim źródłem prawdy o tym, gdzie mieszkaniec pracuje, i rozjechałyby się przy pierwszej zmianie pracy — chyba że ktoś pamiętałby o ich przeliczeniu w każdym miejscu ruszającym etatem. Obietnica niezmienności jest przy tym **mocniejsza** niż przy polu, bo nie da się jej złamać zapomnianym zapisem. Konsekwencja wiążąca dla faz następnych: **faza dokładająca cechę wyglądu dokłada wejście do funkcji, a nie pole do komponentu**; cecha, której nie da się wyprowadzić ze stanu, jest stanem i wtedy wchodzi do hasha na zwykłych zasadach. Punkt 4 domyka przy okazji §7.5 pkt 4 dokumentu M11 („nowe komponenty po stronie sim dopisane do funkcji haszującej") **zbiorem pustym** | M0 (właściciel `core`), M1 (oddaje kształt `RenderSnapshot`), M11 (wnosi), M3/M4 (producenci rekordów), M12 (modding, pamięć) |
| K-17 | Podfazy jako jednostka pracy | **Fazy M2–M12 są rozbite na podfazy** (`MXa`, `MXb`, …), każda we własnym pliku `MX<litera>-<nazwa>.md`. Podfaza to porcja, którą da się zacząć i zamknąć bez trzymania w głowie całej fazy: własny zestaw WP, własny sprawdzalny wynik i własny wycinek §5. Konsekwencje wiążące: (1) opisy pakietów roboczych i sekcje §5 mieszkają w dokumentach podfaz, a §4 i §5 dokumentu fazy są tabelami kierującymi; (2) **numeracja `5.x` jest zachowana** — odesłanie „patrz §5.4" wskazuje tę samą treść, zmienił się tylko plik; (3) zakres (§2), kontrakty międzyfazowe (§6), testy i kryteria akceptacji (§7), ryzyka (§8) i decyzje otwarte (§9) **zostają w dokumencie fazy** i nie są dzielone — kontrakt jest jeden na fazę; (4) **bramki 1–7 z §8 zamykają się na poziomie fazy, nie podfazy** — podfaza zamyka się własnym kryterium. M0 i M1 zostają nierozbite: M0 jest zamknięty, M1 jest w trakcie i przenumerowanie go w locie kosztowałoby więcej, niż daje. **Uzupełnienie (po M2d):** litera zapisuje kolejność **powstania** dokumentu, a kolejność **wykonania** podaje tabela w §4 dokumentu fazy. Podfaza dopisana później wchodzi tam, gdzie jest jej miejsce w zależnościach, i dostaje kolejną wolną literę — nie przenumerowujemy istniejących, bo ich nazwy są cytowane w dzienniku i w tabelach korekt, a zysk byłby wyłącznie estetyczny. Pierwszy przypadek: `M2f` wykonywana między `M2d` a `M2e` | M2–M12 |
| K-32 | `tools/headless` jako biblioteka | **`tools/headless` dostaje target biblioteczny** (`src/lib.rs`), a scenariusze zostają modułami binarki. Wpisane po M5e, jako wykonanie decyzji otwartej nr 10 fazy M5 — i jest to zmiana w crate'cie, którego właścicielem jest **M0**, więc nie rozstrzyga się jej lokalnie w fazie. Wystawione jest wyłącznie to, co ma **więcej niż jednego konsumenta**: `population` (budowa miasta M2 i Etap 8) oraz `retail` (most „zakłady Etapu 7 → rynek detaliczny"). Powód jest ten sam, który stoi za `K-8` i za `kernel` (D20): **druga kopia rusztowania przebiegu rozjeżdża się z pierwszą**, a rozjazd widać dopiero jako inny wynik bramki, czyli w miejscu, w którym najtrudniej powiązać go z przyczyną. Konsumenci po M5e: własna binarka (`m5shop`, `m3day`), `tools/magnat` (gospodarka w oknie, `AB-1`) i `tools/balansator` (przebiegi bramek G1–G9). Konsekwencje wiążące: **(a)** binarka woła bibliotekę przez nazwę pakietu (`use magnat_headless::population`), a nie przez `mod` — dwie kopie modułu dałyby dwa różne typy; **(b)** do biblioteki wchodzi wyłącznie rusztowanie: CLI, raport i kod wyjścia zostają w binarce, bo biblioteka nie ma prawa kończyć procesu; **(c)** zakaz `wgpu` w headlessie (bramka `R-5` w CI) obowiązuje bez zmian i nie osłabia go to, że klient graficzny od tej biblioteki zależy — zależność idzie **do** niej, nie od niej; **(d)** nazwa crate'u („headless") przestała opisywać zbiór konsumentów i jest długiem strukturalnym **przypisanym do R1**, nie do fazy, która ją odziedziczyła | M0 (właściciel `tools/headless`), M5 (wnosi), M6–M12 (przebiegi i balansator) |

| K-43 | Kolejność ról zawodowych i dwa widoki na `data/jobs/roles.ron` | **Kolejność wpisów w `data/jobs/roles.ron` jest kontraktem zapisu gry**, tak samo jak kolejność klas pojazdów (`K-24`) i pul imion (`K-28`). Powód jest ten sam co tam i równie arytmetyczny: `JobRoleId` jest **pozycją roli w tej liście**, a siedzi w komponencie `Employment` każdego mieszkańca i w każdym stanowisku zakładu. Dopisywać wolno **wyłącznie na końcu**; przestawienie albo usunięcie roli przenumerowuje wszystkich zatrudnionych w każdym zapisanym świecie. Dodatkowo `JobTable::role_for` bierze **pierwszą** rolę o danej klasie lokalu, więc przestawienie czterech pierwszych wpisów przebudowałoby obsadę lokali M2. **Drugie rozstrzygnięcie, wykonanie `D16` fazy M7:** plik dostaje pole `weights` (wagi produktywności per zawód, `schema_version` 1 → 2) i **drugiego czytelnika** — `magnat_firms::RoleTable` obok `magnat_world::JobTable`. To jest wyjątek od zwykłej reguły „jedna ładowarka na plik" i ma jawne uzasadnienie: osobny plik z wagami musiałby powtarzać **kolejność** ról, a dwie listy o wymuszonej wspólnej kolejności rozjeżdżają się przy pierwszej zmianie; przeniesienie `JobTable` do `sim/firms` ciągnęłoby za sobą `UnitClass`, czyli gramatykę budynków M2. Dwa widoki na jeden plik rozjechać się nie mogą, bo rozjazd wymagałby przestawienia kolejności, a to psuje oba jednakowo — i pilnuje tego wspólna stała `JOBS_SCHEMA_VERSION` po obu stronach. Katalog urósł w M7a z 4 ról do 46 | M2 (wnosi plik), M7 (wnosi wagi i drugi widok), M3, M8, M12 (modding) |
| K-44 | Pokrycie etatowe jako wejście produkcji | **Korekta macierzy z §1: M7 rozszerza także `sim/supply`**, o jedno pole i jedno mnożenie. `PlantSite` dostaje `labor_pct: u16` (promile, 1000 = obsada kompletna), a `sprobuj_start` mnoży przez nie masę szarży obok ograniczenia z wsadu. Powód jest taki, że bez tego **praca nie jest wejściem produkcji**: do M7a zakład mielił tyle samo z pełną obsadą, z połową i bez nikogo, a płace były kosztem stojącym obok wyniku, nie jego przyczyną — czyli cała warstwa HR z PRD §7.5 nie miałaby jak się objawić w gospodarce. Kierunek zależności jest bezpieczny i jednostronny: `sim/firms` zależy od `sim/supply`, nigdy odwrotnie, więc pisarzem jest M7, a M6 tylko czyta gotową liczbę i **nie zagląda do środka** (M7 §8, ryzyko `R8`). Wartość domyślna 1000 jest neutralna, więc zakład bez przypisanej firmy pracuje pełną parą i żaden test M6 nie zmienił wyniku. Pole wchodzi do hasha stanu, bo zmienia to, ile zakład wyprodukuje. Kto tę liczbę **prowadzi w czasie**: most M7a wpisuje ją raz przy stawianiu miasta, aktualizowanie jej wraz z odejściami i zatrudnieniami przejmuje M7b razem z rynkiem pracy | M6 (właściciel `sim/supply`), M7 (wnosi i pisze), M8 (przerwy w pracy), M10 (strajki, `K-9`) |

| K-45 | Słowniki, które M7 wnosi do `engine/core` | **Wykonanie `K-8` dla M7, wpisane po M7b.** `core` dostaje dwa: **`WageCause`** (dlaczego firma ruszyła stawkę w ofercie pracy — ładunek `DecisionReason::WageRaise`) i **`LeaveCause`** (dlaczego pracownik przestał pracować w tym zakładzie — ładunek `DecisionReason::JobLeft`). Kryterium jest to samo co przy `PriceDriver` (`K-30`) i `LineStopCause` (`K-34`): **ładunek centralnego enuma nie może pochodzić z crate'u, który od `core` zależy**, a oba mają po dwóch czytelników znanych z nazwy i numeru fazy — kartę inspekcji firmy i panel ludzi (M7c), a `WageCause` dodatkowo związki zawodowe (M10, `K-9`), bo „o ile i dlaczego podniesiono" jest wejściem żądania płacowego. Kolejność wariantów jest kontraktem w obu, bo `as_index()` indeksuje histogramy w panelu rynku pracy. **Jedno rozstrzygnięcie warte zapisania:** `WageCause::Ceiling` **nie jest powodem podwyżki, tylko powodem jej braku** — krok przycięty do sufitu marży zapisuje się z przyrostem zero i tym powodem, bo „dlaczego mój wakat stoi pusty" jest pytaniem gracza, na które faza musi umieć odpowiedzieć (M7 §7.1 pkt 4). Osobny słownik na „nie podniosłem" kosztowałby dwa martwe warianty po każdej stronie. Przy okazji, bez potrzeby nowego rozstrzygnięcia: `DecisionReason::{Hired = 500, WageRaise = 501, JobLeft = 502}` otwierają blok M7 (500–599, `K-12`), a `StreamId::{LaborSearch = 220, LaborQuit = 221}` blok M7 strumieni (220–239, `K-4`). Wszystkie te wartości są od tej chwili wieczne | M0 (właściciel `core`), M7 (wnosi), M8, M9, M10 |
| K-46 | Jeden identyfikator zakładu w przestrzeni gospodarki | **`SiteId` zakładu to `SITE_KEY_BASE` + indeks w `city.sites.sites`** — ten sam klucz, którym posługują się sklep M5, zakład produkcyjny M6, komponent `magnat_agents::Employment` mieszkańca i rejestr firm M7. Generator miasta numeruje zakłady **od zera** i tym numerem oznacza lokale (`UnitOccupant::Site`), więc istnieją dwie numeracje i obie są poprawnymi `SiteId` — przesunięcie jest jedyną rzeczą, która je odróżnia. Wpisane po M7b, bo tam koszt pomyłki stał się widoczny: most M7a nadawał `Site.id` w numeracji generatora, a `Employment.site` niesie klucz przesunięty, więc **ten sam zakład był dla kodu dwoma różnymi miejscami**. Objawiłoby się to dopiero jako zakład, w którym rynek pracy zwalnia całą załogę w pierwszej dobie („ten człowiek pracuje gdzie indziej"), albo jako pokrycie etatowe wpisane do cudzej linii produkcyjnej. **Reguła wiążąca:** przestrzeń generatora zostaje wewnątrz `sim/world` i służy wyłącznie do wiązania zakładu z lokalami; **wszystko, co przechodzi granicę crate'u, używa klucza przesuniętego**. Konwersja jest w jednym miejscu po każdej stronie (`headless::plants::site_id`, `agents::places::place_from_key`) i nowa faza nie wymyśla własnej | M2 (generator), M3 (klucz miejsca), M5, M6, M7, M8 |

---

## 5. Dane (`data/`)

- Format: **RON**. Ładowane przy starcie, walidowane przed użyciem.
- Katalogi: `data/goods/`, `data/recipes/`, `data/jobs/`, `data/buildings/`, `data/grammar/`,
  `data/names/` (nazwy własne generowane proceduralnie: dzielnice, firmy oraz imiona
  i nazwiska mieszkańców per region — `K-28`, właściciel katalogu imion M4),
  `data/epochs/`, `data/events/`, `data/needs/` (tabela potrzeb M3 oraz
  `categories.ron` — kategorie potrzeb `NeedCategoryId`, `K-34`, właściciel M6),
  `data/demography/`,
  `data/materials/`, `data/geology/`, `data/climate/`, `data/zoning/`, `data/districts/`,
  `data/chains/` (szablony łańcuchów przemysłowych i koszyk potrzeb epoki — M2;
  `external.ron` — progi wejścia i kapitał sieci zewnętrznych, **właściciel M7**,
  dopisany po M7f: kolejność wpisów jest kontraktem zapisu gry, bo indeks wiersza
  jest `ExternalInvestorId` w dzienniku transakcji), `data/site_types/`, `data/scenarios/` (dwa pliki o dwóch właścicielach:
  `initial_stock.ron` — zapas startowy zakładów Etapu 7, cztery liczby w dobach,
  nie tabela per towar; **zapas startowy nie jest źródłem w grafie produktów**,
  walidator go nie widzi (M6 §6.4.1), właściciel M6. Oraz `scenarios.ron` —
  scenariusze rozgrywki: klucz, tytuł i opis jako `LocKey`, łatki świata, cele
  i limit czasu; **kolejność wpisów jest kontraktem zapisu gry**, bo `ScenarioId`
  w kopercie `StartGame` jest pozycją w tej liście (`K-71`), właściciel M9),
  `data/locale/`,
  `data/policies/` (presety reguł jako wzorce polityk: `presets.ron` — klucz tekstowy
  jest kontraktem zapisu gry, numer nadaje dopiero przypisanie do zakładu; każdy preset
  przechodzi walidator `sim/policy` **przy ładowaniu**, a preset niepoprawny zatrzymuje
  ładowanie, bo polityka wyglądająca na działającą i nic nierobiąca jest gorsza
  od komunikatu o błędzie; `K-47`, właściciel M7, edytor i format tekstowy M9.
  **Od M7e klucz presetu jest też odwzorowaniem kursu firmy** (`FirmStrategy` z `K-49`):
  tier taktyczny przypina zdelegowanemu zakładowi preset odpowiadający kursowi, więc
  usunięcie klucza z tego pliku zostawia menedżera bez reguł),
  `data/palettes/` (zestawy barw per rodzaj dzielnicy × epoka, `palettes.ron`;
  **kolejność osi `districts` i `epochs` jest kontraktem zapisu gry**, bo indeks pary
  jest `DistrictPaletteId` w buforze instancji — `K-76`, właściciel M11),
  `data/models/` (modele voxelowe encji i propów w formacie binarnym `.mvox`,
  kompilowane przez `tools/mvoxc`; `ModelId` jest pozycją posortowanego klucza,
  więc w zapisie gry trzymamy klucz — `K-76`, właściciel M11),
  `data/audio/`, `data/tech/` (drzewa technologii, M10),
  `data/tuning/` (kalibracja, którą stroi balansator: `supply.ron` — progi kaskady
  niedoboru, taryfy mediów, zużycie maszyn, stawki transportowe i stała odniesienia
  skali emisji, `K-35`, właściciel M6; `labor.ron` — parametry licytacji o pracownika,
  płacy progowej, rotacji i świadczeń, właściciel M7; `insolvency.ron` — progi
  niewypłacalności, przebieg wyprzedaży masy i wycena lotów, właściciel M7,
  dopisany po M7d. **Kolejności zaspokojenia w nim nie ma i nie będzie**:
  jest regułą podziału, nie kalibracją, i siedzi w `ClaimPriority` (`K-48`). **Tabeli płac w `labor.ron` nie
  ma i nie będzie**: widełki roli mieszkają w `data/jobs/roles.ron` i są przedziałem,
  w którym firma licytuje; `grid.ron` — taryfy operatorów sieci przesyłowych,
  dobowy kształt popytu, czas naprawy i wymiarowanie sieci, właściciel M8,
  dopisany po M8b. **Taryfa stoi tu, a nie w `data/city/`**, bo jest ceną
  operatora, a nie daniną — uchwała ograniczająca ją (`Policy::TariffCap`, M8e)
  będzie w `data/city/`, ale sama cena nie jest polityką fiskalną;
  `city.ron` — kalibracja usług publicznych, urzędów i egzekucji: wagi jakości
  placówki, normy finansowania na osobę, zanik pokrycia, przerób urzędu,
  parametry szarej strefy i progi pięciu urzędów kontrolnych, `K-65`,
  właściciel M8, dopisany po M8d;
  `policy.ron` — krzywa „umiejętność zarządzania → jakość wykonania polityki”:
  opóźnienie informacji, zwłoka reakcji, rozrzut wykonania i szansa pominięcia
  cyklu, po dwa krańce na każde, plus podłoga „bez nadzoru”; `K-70`,
  właściciel M9, dopisany po M9d),
  `data/city/` (polityka fiskalna miasta: `tax.ron` — siedem danin, klasy VAT
  przypisane przez domenę klucza towaru, stawki akcyzy, opłaty koncesyjne, terminy
  i odsetki; `budget.ron` — udziały planu wydatków, rezerwa, próg cięcia i parametry
  emisji obligacji; `K-56`, właściciel M8),
  `data/ui/` (palety i progi nakładek danych — `K-19`, właściciel M2),
  `data/trade/` (klasy taryfowe obrotu zagranicznego i ich stawki: `tariffs.ron` —
  przypisanie przez domenę klucza towaru, stawki jako stub do czasu M8; `K-37`,
  właściciel M6, stawki przejmuje M8),
  `data/vehicles/` (klasy pojazdów i mnożniki wzoru paliwowego — `K-24`, właściciel M4),
  `data/roads/` (parametry ruchu: diagram podstawowy i wybór środka transportu —
  `K-24`, zakres doprecyzowany w `K-27`; właściciel M4),
  `data/economy/` (parametry gospodarki detalicznej: `weights.ron` — wagi funkcji
  użyteczności zakupu per potrzeba, `choice.ron` — temperatura softmaxu, szum, progi
  odłożenia zakupu, promień zapytania i budżety odniesienia, `retail.ron` — kategoria
  zapasu, trwałość, czas dostawy i narzut per towar oraz asortyment per rodzaj sklepu,
  `shop.ron` — osobowość cenowa firmy handlowej (widełki marży, czułość na zapas
  i na konkurencję, skłonność do eksperymentu, czujność 1–7 dni, progi przeceny
  psującego się) oraz koszty stałe zakładu (czynsz, media, płace, wyposażenie
  i okres amortyzacji), dopisane po M5c;
  `envelopes.ron` — koszty stałe gospodarstwa domowego i wagi kopert per typ
  gospodarstwa, `bank.ron` — ocena zdolności kredytowej, produkty kredytowe
  i reguła stopy bazowej banku centralnego, `cpi.ron` — koszyk miejski `q_0`
  do indeksu Laspeyresa, dopisane po M5d;
  właściciel M5. `choice.ron`, `shop.ron`, `envelopes.ron` i `bank.ron` są plikami,
  które stroi **balansator**, i od M5e to on odpowiada za ich wartości —
  `min_margin_bp` z `shop.ron` jest dolnym ogranicznikiem ceny, czyli mechanizmem,
  którego pilnuje bramka G3, a `a_bp` i `max_step_bp` z `bank.ron` decydują o tym,
  czy pętla kredytowa się stabilizuje (bramki G1–G2). **`cpi.ron` jest wyjątkiem:
  nie stroi go nikt**, bo `q_0` jest bazą indeksu i jego zmiana przestawia całą
  historię CPI).
  Faza dokładająca katalog dopisuje go tutaj — to jest lista kompletna, nie przykładowa.
- Każdy plik ma `schema_version`.
- **Walidator grafu produktów** (każdy towar ma źródło) wchodzi do CI **już w M2**, nie w M6 —
  Etap 7 generacji (obsadzenie budynków firmami) musi sprawdzić domknięcie łańcuchów, zanim
  powstanie jakakolwiek produkcja. M2 tworzy minimalny katalog `data/goods/` i `data/recipes/`
  oraz walidator domknięcia osiągalności; **M6 jest właścicielem docelowego schematu i pełnego
  katalogu (~400 towarów)** i rozszerza jedno i drugie. M2 nie projektuje schematu pod siebie —
  uzgadnia go z M6. **Wykonane w M6a:** katalog i walidator wyprowadziły się z `sim/world`
  do `sim/supply` i `tools/goods-graph` (`cargo test -p goods-graph`), a klucze towarów
  przeszły na konwencję `<domena>_<nazwa>[_<wariant>]` z zamkniętą listą dwunastu domen.
- `GoodId`/`RecipeId` nadawane przy ładowaniu (stabilna kolejność alfabetyczna klucza tekstowego);
  w zapisie gry trzymamy klucz tekstowy, nie indeks.

---

## 6. Konwencje kodu i testów

- Rust 2021, `#![forbid(unsafe_code)]` domyślnie; `unsafe` tylko w `ecs`/`voxel`,
  z uzasadnieniem w komentarzu i testem pod Miri.
- Każdy crate: `cargo test` zielony, `clippy -D warnings`, `criterion` dla ścieżek gorących.
- **Headless-first:** każdy system symulacji musi dać się uruchomić i przetestować bez GPU.
- Testy własnościowe obowiązkowe dla każdej fazy dotykającej pieniądza lub masy:
  - suma pieniądza = emisja − destrukcja,
  - suma masy towaru = produkcja − konsumpcja − straty.
- Nazewnictwo: kod i identyfikatory po angielsku, dokumentacja i komentarze domenowe po polsku.

---

## 7. Wymóg wyjaśnialności (PRD §14.1, §20.1)

Każda decyzja agenta lub firmy zapisuje **powód** w formie strukturalnej
(`DecisionReason` — enum z parametrami, nie string), dostępny w karcie inspekcji.
System bez wyjaśnialności nie spełnia Definition of Done swojej fazy.

---

## 8. Szablon dokumentu fazy

Każdy plik `MX-*.md` ma dokładnie te sekcje:

1. Cel fazy i artefakt końcowy (co da się uruchomić i zobaczyć po zakończeniu)
2. Zakres — wchodzi / nie wchodzi (z odesłaniem do fazy, która to robi)
3. Mapowanie na PRD (numery sekcji)
4. Pakiety robocze (WP) — kolejność, zależności, opis, kryterium ukończenia
5. Projekt techniczny — struktury danych, sygnatury API, systemy ECS i ich częstotliwość
6. Kontrakty międzyfazowe — „dostarczam" / „konsumuję" (jawne nazwy typów i funkcji)
7. Testy i kryteria akceptacji (w tym determinizm i wydajność)
8. Ryzyka fazy i mitygacje
9. Decyzje otwarte (do rozstrzygnięcia przed startem fazy)
10. Szacunek wielkości (rozmiar względny WP: S/M/L/XL, bez dat)

Od M2 sekcje 4 i 5 są **rozbite na dokumenty podfaz** (`K-17`): w dokumencie fazy zostają tabele
kierujące, a opis pakietów i treść §5 mieszkają w plikach `MX<litera>-<nazwa>.md`. Numeracja `5.x`
nie zmienia się przy przenosinach. Pozostałe sekcje zostają w dokumencie fazy w całości.
